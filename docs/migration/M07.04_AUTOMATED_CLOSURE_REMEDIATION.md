# M07.04 Automated Closure Remediation

The independent M07.03 strict audit reopened closure because repository source
evidence contradicted the M07.03 Codex-log claims. M07.04 is the only active
remediation scope. It preserves the M07.03 Command Center UI and addresses
rescan truthfulness, watcher evidence, registry identity continuity, schema v7,
SQLite backup durability, Git diff boundary behavior, and production launcher
provenance/readiness/rollback.

Implemented in this pass:

- explicit watcher reconciliation now clears `rescan_required` only after an
  available successful reconciliation and persists the same value in memory;
- non-Git registrations retain a repository identity row, and no-remote repair
  accepts only exact or read-only ancestor continuity;
- migration v7 converts legacy `project_snapshots.created_at` values;
- migration backup rotation retains bounded `.bak` and `.bak.prev` paths;
- binary patch fallback classification is limited to the current diff file;
- the publisher always runs Tauri production `--no-bundle`, emits no bypass
  switch, waits for the post-mount `HIVEAI_FRONTEND_READY` marker, cleans up
  candidate processes, and smoke-tests the stable path after swap.

Verification completed: frontend typecheck/test/build and high-severity npm
audit passed; Rust format/check/test/build passed; the publisher passed its
production candidate and stable-path readiness smoke. The focused evidence
matrix remains bounded to committed fixtures and is subject to independent
audit; no unconditional acceptance is claimed.

The required focused evidence and full regression commands are recorded in the
new M07.04 Codex log. Automated closure remains subject to independent audit;
visual status remains **PENDING USER VISUAL ACCEPTANCE**, and M01 restart
acceptance remains manual/unverified.
