# M07.05 - H!veAI Bounded Correctness, Evidence + Manual QA Remediation

You are continuing H!veAI after the independent M07.04 strict audit returned **FAIL** and after new user manual-QA screenshots exposed additional UI/data-integrity defects.

This is the single authoritative M07.05 remediation pass. It is **not M08**.

Do NOT start M08.
Do NOT create an M08 prompt.
Do NOT create a Windows installer.
Do NOT ignore the new manual-QA requirements merely because M07.03 previously froze the UI. The user has now explicitly requested the exact bounded UI corrections in R09-R11 below.

The goal is to close the independently verified automated blockers/majors, repair the newly observed project-identity/UI defects, preserve the canonical single-viewport dashboard composition, and produce production-path evidence.

---

## Mandatory fetch-before-prompt sync

From:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ`

run:

```powershell
git fetch origin H!veAI
git rev-list --left-right --count HEAD...origin/H!veAI
```

If local is safely behind with no conflicting tracked changes:

```powershell
git merge --ff-only origin/H!veAI
```

Never reset, force checkout, automatically rebase, or force push.

Canonical branch: `H!veAI`

Canonical app root:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI`

Preserve unrelated stash/untracked user files including parent `start-demo.bat` and `task.md`.

---

# Canonical UI Assets

Canonical UI reference assets are located at:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI`

Use the assets in this folder as authoritative visual references:

- dashboard reference image
- Akilta logo
- H!veAI logo
- `H!veAI small logo.png`
- `H!veAI text logo.png`

Do not redesign, recolor, crop, stretch, replace, or reinterpret these assets.

Preserve the sidebar brand composition:

```text
[ H!veAI small logo.png ] [ H!veAI text logo.png ]
```

Read and obey the newly tightened durable governance in:

`H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`

That document now includes the latest user manual-QA requirements for:

- live Project Registry identity as UI source of truth;
- no FormuLab fallback flicker for registered project routes;
- slightly narrower sidebar;
- distinct topbar Command / AI Assistant / Notifications surfaces;
- no visible nested scrollbars in the canonical core cockpit blocks;
- Global Overview without a visible WORKSPACE OVERVIEW eyebrow;
- compact current-project identity row;
- bottom-center global Akilta footer with red heart.

This Canonical UI Assets section is mandatory in addition to `H!veAI/AGENTS.md`.

---

## Read completely before coding

Read:

- `H!veAI/AGENTS.md`
- `H!veAI/CONSTITUTION.md`
- `H!veAI/ARCHITECTURE.md`
- `H!veAI/TASKS.md`
- `H!veAI/CODEX_ROADMAP.md`
- `H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`
- `H!veAI/docs/H!veAI/audits/M07.04_AUTOMATED_CLOSURE_REMEDIATION_STRICT_AUDIT.md`
- `H!veAI/docs/H!veAI/prompts/M07.04_AUTOMATED_CLOSURE_REMEDIATION_PROMPT.md`
- `H!veAI/docs/H!veAI/codex-logs/M07.04_AUTOMATED_CLOSURE_REMEDIATION_CODEX_LOG.md`
- this prompt

Inspect actual source before editing, especially:

- `H!veAI/src-tauri/src/watcher.rs`
- `H!veAI/src-tauri/src/projects/registry.rs`
- `H!veAI/src-tauri/src/db/migrations.rs`
- `H!veAI/src-tauri/src/db/mod.rs`
- `H!veAI/src-tauri/src/git_engine/mod.rs`
- `H!veAI/src-tauri/src/lib.rs`
- `H!veAI/src/pages.tsx`
- `H!veAI/src/components/Shell.tsx`
- `H!veAI/src/command-center.css`
- `H!veAI/src/styles.css`
- `H!veAI/src/fixtures.ts`
- `H!veAI/src/projectRegistry.ts`
- routing / shared state files used by the app.

Treat aggregate test counts as insufficient. Every required direct test below must exist and exercise the production boundary named.

---

## Durable M07.05 log

Create before implementation:

`H!veAI/docs/H!veAI/codex-logs/M07.05_BOUNDED_CORRECTNESS_AND_EVIDENCE_REMEDIATION_CODEX_LOG.md`

Do not edit historical Codex logs.

For every finding record:

- finding ID;
- source file/symbol;
- previous incorrect behavior;
- exact correction;
- exact test names;
- whether tests call production functions or only helpers;
- failures encountered while implementing;
- full regression commands/results;
- Windows launcher evidence;
- manual-QA UI evidence/status;
- commits/push/final remote HEAD;
- remaining manual-only gates.

---

# R01 - BLOCKER - Rescan may clear only on successful explicit recovery

Primary file:

`H!veAI/src-tauri/src/watcher.rs`

Current bad expression confirmed by independent audit:

```rust
let successful_reconciliation = available && (explicit || !events.is_empty());
```

A normal event refresh with a non-empty event list can therefore clear a previously required rescan.

## Required production behavior

1. ordinary refresh (`explicit == false`) must **never** clear `prior_rescan_required == true`, regardless of event count;
2. explicit rescan may clear only after all required recovery steps succeed;
3. project root must be available;
4. watcher attachment/configuration must be successful for an available project;
5. Git snapshot refresh, when applicable, must succeed;
6. project snapshot persistence must succeed;
7. persisted final `rescan_required` must equal in-memory final state;
8. missing/unavailable root must not report recovery;
9. watcher creation/watch failure must not report recovery;
10. failed Git/persistence path must preserve the recovery requirement when it was already true.

Do not define recovery success from event-vector emptiness.

## Architecture guidance

Make watcher configuration return a meaningful attachment result instead of converting watcher attachment failure into ambiguous `Ok(())`.

A small internal state such as:

```rust
enum WatchAttachmentState {
    Watching,
    Missing,
    Failed,
}
```

or an equivalent bounded design is acceptable.

Compute one final desired watcher state, persist that state, then publish that exact state in memory.

## Required direct tests

Use temp DB/project fixtures and production functions:

1. overflow/error marks rescan true;
2. ordinary refresh with **non-empty event vector** preserves true;
3. ordinary refresh with empty event vector preserves true;
4. healthy explicit rescan clears true;
5. DB newest row equals memory after successful explicit rescan;
6. missing-root explicit attempt preserves true and degraded/missing state;
7. watcher attachment failure preserves true;
8. Git refresh failure preserves true;
9. persistence failure preserves true;
10. healthy explicit rescan when no rescan was required is deterministic.

Do not manually clear status in tests after bypassing production logic.

---

# R02 - MAJOR - Watcher repaired-root reattachment + focused evidence

Current defect:

`configure_project()` returns early when the project ID already exists in `inner.watches`, but the map does not persist the attached normalized root. A Project Registry repair can therefore leave a live watcher attached to the old directory.

## Required production behavior

- record the normalized root associated with each active watcher;
- when the registered root changes for the same project ID, drop/remove the old watcher and attach a new watcher to the new root;
- repeated refresh with unchanged root must not create duplicate watchers;
- missing root removes the active watcher but retains degraded registry/watcher state;
- repaired valid root reattaches deterministically.

## Required direct tests

1. positive in-root rename old/new relative paths;
2. rename with either endpoint outside root fails closed;
3. duplicate watcher prevention on repeated registry refresh;
4. repair old root -> new root -> refresh -> watcher attached to new root;
5. event created on repaired new root is observed/processed;
6. Git-category event produces bounded persisted Git snapshot refresh;
7. non-Git event does not attempt Git snapshot persistence;
8. initialization with one missing registered project still returns manager with degraded project state;
9. manager drop joins/releases worker/watchers;
10. IPC/capability surface has no arbitrary watch-path/file-read command;
11. lexical sibling/`..` reject;
12. physical escape reject where platform fixture permits;
13. nonexistent-path nearest-existing-ancestor containment behavior.

All filesystem fixtures must be temp directories.

---

# R03 - BLOCKER - Legacy non-Git repair must never fail open

Primary file:

`H!veAI/src-tauri/src/projects/registry.rs`

M07.04 creates explicit `is_git_repository=0` repository rows for new non-Git registrations. Historical projects registered before that change may still have no repository row.

## Required production behavior

For an existing project ID:

- repository row present with `is_git_repository=0` -> registered non-Git identity;
- repository row absent due legacy registration -> also treat as registered non-Git identity, never unknown/fail-open;
- non-Git -> non-Git repair may succeed;
- non-Git -> Git repair rejects;
- unknown project ID rejects;
- Git identity validation remains strong and read-only.

Do not require schema v8 solely for this if legacy state can be interpreted safely at runtime.

### Strong remote identity

- same sanitized remote + same HEAD -> accept;
- same sanitized remote + advanced HEAD -> accept;
- different remote -> reject;
- remote disappears -> fail closed unless an explicit strong history-continuity rule proves identity.

### No usable remote

- exact old HEAD -> accept;
- old HEAD ancestor of candidate HEAD -> accept;
- old HEAD missing -> reject;
- unrelated history -> reject.

Use fixed arguments for `git merge-base --is-ancestor` or equivalent. Never use shell command strings and never fetch/pull/checkout/reset/config-mutate the external repository.

## Required tests

All must call real `repair_project_path()`:

1. legacy non-Git project with no repositories row -> non-Git moved path accepted;
2. same legacy non-Git project -> Git path rejected;
3. current explicit non-Git -> Git rejected;
4. same remote/same HEAD accepted;
5. same remote/advanced HEAD accepted;
6. different remote rejected;
7. remote disappearance policy tested;
8. no-remote exact HEAD accepted;
9. no-remote ancestor accepted;
10. unrelated no-remote rejected;
11. missing old HEAD rejected;
12. credential-bearing equivalent HTTP(S) remotes compare equal after sanitation;
13. duplicate normalized repair path rejected;
14. unknown project ID rejected;
15. candidate repo files/`.git` evidence unchanged by validation itself.

---

# R04 - MAJOR - Migration v7 proof + historical migration immutability

Primary file:

`H!veAI/src-tauri/src/db/migrations.rs`

M07.04 correctly introduced published migration 7:

- version: `7`
- name: `project_snapshot_created_timestamp`

Do not rename migration 7. The user's local DB may already contain it.

Do not introduce v8 for this correction.

Restore historical migration 5 to its exact pre-M07.04 published behavior by removing the newly-added `project_snapshots.created_at` update from `TIMESTAMP_STANDARDIZATION`.

Leave the actual missing-field correction only in migration 7.

Do not rewrite migration 6.

## Required tests

Use real `apply_migrations()`:

1. UTC timestamp generator parses RFC3339;
2. millisecond precision;
3. canonical timestamps sort lexicographically in chronological order;
4. fresh DB reaches v7 / seven rows;
5. apply migrations 1..6, insert numeric `project_snapshots.created_at`, then v7 -> converts correctly;
6. valid RFC3339 `project_snapshots.created_at` remains byte-for-byte unchanged;
7. malformed legacy string remains preserved;
8. relevant Registry/Git/Watcher legacy timestamp examples remain correct;
9. rerun -> `ALREADY_CURRENT`;
10. migration history mismatch fails safely;
11. intentional migration failure rolls back.

---

# R05 - MAJOR - SQLite durability evidence must exist

Primary file:

`H!veAI/src-tauri/src/db/mod.rs`

Preserve SQLite-native `rusqlite::backup` and bounded `.bak` / `.bak.prev` policy.

Production code may be refactored only enough to make failure behavior directly testable without exposing a public runtime bypass.

## Required direct tests

1. committed WAL content exists in logical backup;
2. second backup succeeds while prior `.bak` exists;
3. retention stays bounded;
4. simulated backup publication failure leaves source logical data/bytes unchanged as appropriate;
5. previous good backup remains recoverable after publication failure;
6. busy/locked contention returns predictably within configured bound;
7. corrupted DB initialization fails;
8. corrupt source bytes remain byte-for-byte unchanged;
9. healthy `quick_check` succeeds;
10. every `DatabaseState::open_connection()` has intended FK/WAL/busy/synchronous policy;
11. backup failure aborts migration before schema mutation.

Use temp DBs only.

Failure injection may use private/internal helpers or `#[cfg(test)]`. Do not create public runtime bypass flags.

---

# R06 - MAJOR - Real H!veAI diff() matrix + deterministic binary metadata

Primary file:

`H!veAI/src-tauri/src/git_engine/mod.rs`

Current human-readable `Binary files ...` parsing is not sufficient for staged newly-added binaries because one side can be `/dev/null`.

## Required production behavior

- use matching-scope bounded metadata command such as `git diff --numstat` to identify binary paths (`-\t-` rows);
- text diff remains separate and uses safe fixed arguments;
- include `--no-ext-diff` and `--no-textconv` for text diff;
- never request raw binary patch for product output;
- retain sanitizer fallback for unexpected `GIT binary patch` sections;
- sanitize before truncation;
- returned binary paths are real repository-relative paths, never `/dev/null`;
- mixed text+binary keeps text hunks and only binary metadata for actual binary files.

## Required tests MUST call real `diff()`

Build temp Git repos, register through H!veAI DB/Registry, then call:

```rust
diff(&database, GitDiffRequest { ... })
```

Required matrix:

1. text working tree;
2. text staged;
3. binary working tree;
4. newly-added binary staged -> actual file path, not `/dev/null`;
5. mixed text+binary working tree;
6. mixed text+binary staged;
7. parser-level injected binary patch sanitation;
8. injected mixed patch does not misclassify text file;
9. returned text excludes `GIT binary patch`, `literal`, `delta`, base85 body;
10. truncation reflects sanitized text;
11. no NUL/raw binary bytes in returned text.

Rename/remove any test whose name claims product-boundary coverage while only testing raw Git output plus helper sanitation.

---

# R07 - MAJOR - Dedicated frontend readiness signal must be unambiguous

Files:

- `H!veAI/src-tauri/src/lib.rs`
- `H!veAI/src/main.tsx`
- Tauri permission/capability files
- `H!veAI/scripts/publish-dev-qa.ps1`

Preserve the post-render React call to `hiveai_frontend_ready`.

## Required production behavior

- `HIVEAI_FRONTEND_READY` is emitted only by the dedicated readiness command;
- remove that exact marker from `hiveai_native_status` and `hiveai_database_status`;
- explicitly include readiness command in the intended narrow Tauri permission/capability policy where ACL requires it;
- no arbitrary log/file/shell IPC;
- publisher records pre-launch log baseline and waits for a new marker;
- 5173 and 8765 absent;
- candidate remains alive until readiness proven;
- cleanup bounded on every failure.

Add source/permission regression evidence proving the marker has one Rust emission site.

---

# R08 - MAJOR - Transactional launcher rollback must be hash-proven and tested

Primary file:

`H!veAI/scripts/publish-dev-qa.ps1`

Production publisher must retain:

- no `-SkipBuild`;
- no external Candidate parameter;
- self-run `npm run tauri:build -- --no-bundle`;
- candidate readiness smoke before swap;
- stable-path readiness smoke after swap;
- direct Desktop shortcut target;
- canonical shortcut icon;
- no installer.

## Required hash behavior

Before replacing an existing stable EXE:

- record SHA-256 of previous stable;
- on post-swap failure restore rollback;
- verify restored stable SHA-256 exactly equals pre-swap hash;
- hash mismatch is a hard error preserving available evidence.

## Required controlled failure harness

Create a separate safe test helper/harness targeting temp paths only and incapable of publishing to the real Desktop stable file.

Required cases:

1. invalid PE -> stable unchanged;
2. provenance/build failure -> stable unchanged;
3. readiness failure -> stable unchanged;
4. shortcut/icon precondition failure -> stable unchanged;
5. simulated post-swap failure -> rollback restored and hash equal;
6. locked stable -> clear bounded failure + unchanged bytes;
7. failed smoke leaves no candidate process;
8. successful temp swap -> final hash equals candidate hash;
9. production publisher interface has no bypass flags;
10. final real publication after all project tests -> stable-path readiness smoke passes.

Do not add a test-only bypass to the real production publisher interface.

---

# R09 - BLOCKER - Eliminate fixture/live Project Registry identity mixing

New user manual-QA evidence showed:

- a newly registered `AI-Commerce-HQ` project appears on the real Projects page but not on the Command Center Projects rail;
- the Command Center still displays FormuLab/FMCG ERP/Scrubbots/PackLab 3D fixture identities;
- sidebar Project Shortcuts remain hardcoded fixture buttons;
- `Total projects` and related project identity counts are presented as if live while they are static fixture values;
- Current Project is hardcoded as FormuLab.

Source evidence before this remediation:

- `pages.tsx` imports `projects` from `fixtures.ts` and maps that array into the Command Center;
- `Shell.tsx` hardcodes `/projects/formulab`, `/projects/fmcg-erp`, `/projects/scrubbots`;
- `fixtures.ts` explicitly states its project data is static M02 presentation data.

## Required architecture

In **Tauri desktop mode**, persisted Project Registry identity is the single source of truth for project identity surfaces.

Implement one bounded shared live-registry view model/provider/store/hook or equivalent so these surfaces observe the same project set and refresh signal:

1. Projects page;
2. Command Center Projects rail;
3. Sidebar Project Shortcuts;
4. Current Project identity/header;
5. Project Cockpit route resolution.

Do not create several independently stale copies.

A successful register/repair/archive/remove operation must trigger registry refresh for other mounted surfaces without restarting H!veAI.

### Required Command Center behavior

- Projects rail uses real registered projects in Tauri desktop mode;
- newly registered project becomes visible in the rail without app restart;
- project row click routes using real registered project ID;
- real project name/path/status/Git identity is never replaced by fixture identity;
- registered project count comes from real registry;
- other task/health/progress fields that are not yet available may be clearly labeled placeholders/unknowns until later milestones, but must never impersonate another project;
- if more registered rows exist than the fixed one-screen rail can comfortably show, use a compact deterministic policy that keeps recent/active registered projects visible and provides `View all projects`; do not silently keep only the old fixture list.

### Required sidebar behavior

Replace hardcoded fixture Project Shortcuts with real registered-project shortcuts or a compact derived subset from the same registry state.

- new project should become eligible/visible without restart;
- shortcut routes use real project IDs;
- no hardcoded FormuLab/FMCG/Scrubbots paths in desktop live mode;
- browser preview may retain explicitly labeled fixtures if isolated from Tauri production behavior.

### Required Current Project behavior

- Current Project identity comes from a real registered project in Tauri desktop mode;
- if user opens/selects a project, preserve that selection when returning to Command Center during the session;
- if no project has been selected, choose a deterministic registered-project default, not `fixtures[0]`;
- task/workflow placeholders may remain until M08/M09, but they must be neutral for a project with no parsed task data and must not show FormuLab-specific task text under AI-Commerce-HQ.

## Required frontend tests

At minimum add tests proving:

1. Tauri Command Center renders registered project names from mocked registry API rather than fixture identities;
2. newly registered project refresh appears in Command Center rail;
3. sidebar shortcuts derive from registry data;
4. a real AI-Commerce-HQ registry fixture never renders `FormuLab` as its identity;
5. project count reflects live registry fixture count;
6. archive/remove refresh removes project from live rail/shortcut surfaces;
7. browser-preview fixture behavior, if retained, is explicitly isolated from Tauri live mode.

---

# R10 - BLOCKER - Project Cockpit route must never flash or fall back to FormuLab

New manual-QA evidence showed a newly registered project cockpit first rendered FormuLab and later changed to AI-Commerce-HQ after the asynchronous registry request completed.

Source defect before remediation:

```ts
const project = projects.find(item => item.id === id) ?? projects[0];
```

followed by async:

```ts
getRegisteredProject(id).then(setRegistered)
```

This creates a fixture fallback race. The user observed approximately 20 seconds before the true project replaced FormuLab. There is no requirement for any such identity flip.

## Required Tauri route state machine

Use explicit route-resolution state, e.g.:

```text
LOADING_REGISTERED_PROJECT
REGISTERED_PROJECT_READY
NOT_FOUND
ERROR
```

or equivalent.

Rules:

1. while registered-project lookup is pending, render Loading/Skeleton only;
2. loading UI must contain no FormuLab, AI-Commerce-HQ, or other unrelated project identity;
3. once resolved, keep the requested project identity stable;
4. if lookup fails because ID is not registered, show a clear not-found/error state;
5. never use `projects[0]` as an implicit Tauri registered-project fallback;
6. clicking `Open cockpit` on a newly registered project opens that exact project;
7. asynchronous Git snapshot loading must not replace/reset the project identity;
8. route changes must cancel/ignore stale previous lookup results so an older request cannot overwrite a newer route;
9. fixture preview routes, if still needed for browser preview, must be explicitly separated from production registered-ID resolution.

## Required tests

1. pending registered lookup shows Loading and **does not contain FormuLab**;
2. resolved AI-Commerce-HQ route shows AI-Commerce-HQ directly;
3. delayed registry promise cannot show FormuLab during delay;
4. changing route IDs while first lookup is pending cannot let stale result overwrite new route;
5. unknown registered ID -> not-found/error, not FormuLab;
6. Open cockpit from new Projects card routes to the new project's ID;
7. Git status async completion does not alter cockpit project identity.

---

# R11 - MAJOR - Exact user-directed Command Center visual/interaction corrections

The user supplied annotated manual-QA screenshots after restarting through the real Desktop `H!veAI.lnk`.

Implement these exact corrections while preserving the canonical reference layout.

## R11.1 Remove visible WORKSPACE OVERVIEW eyebrow

Current:

```text
WORKSPACE OVERVIEW
Global Overview
```

Target:

```text
Global Overview
```

The visible `Workspace overview` eyebrow is not needed. Accessibility-only hidden labels are fine.

## R11.2 Narrow the main sidebar slightly

Current CSS uses approximately 244 px.

Target approximately `216-224px`, choosing the smallest width that still preserves:

- fully visible small H!veAI logo;
- fully visible H!veAI text-logo;
- readable nav labels;
- usable click targets.

Do not shrink/distort branding assets.

## R11.3 Compact Current Project header

Do not stack a large project-name hero beneath `CURRENT PROJECT` when it can fit inline.

At canonical desktop width use a compact row equivalent to:

```text
CURRENT PROJECT   <Project Name>                         <status badge>
```

Keep secondary description/phase as a smaller second line only if needed.

This row must use the actual live registered project identity from R09.

## R11.4 Remove visible nested scrollbars from core cockpit blocks

At `1536x1024` and `2048x1280` the following Command Center blocks must show **no visible internal vertical scrollbar**:

- Current Task;
- Workflow Status;
- Recent Activity;
- Project Metrics;
- System Status.

Current CSS has `overflow:auto` on several of these surfaces. Do not merely hide the scrollbar while keeping inaccessible clipped content.

Fit content by:

- reducing vertical padding/gaps;
- tightening typography within readable bounds;
- compacting task/workflow rows;
- showing bounded meaningful summary rows;
- moving deep System Status details out of the compact main panel into a dedicated popover/modal/detail view if necessary;
- preserving the canonical one-screen composition.

The reference dashboard proves the information can be presented densely without these visible nested scrollbars.

Do not reintroduce outer Command Center scrolling.

## R11.5 Distinct topbar actions

Current source wires Search/Command/Sparkles/Bell into the same command-palette state.

Required:

- Search field and Command icon -> Command Palette;
- Sparkles icon -> distinct AI Assistant panel/drawer/popover;
- Bell icon -> distinct Notifications panel/drawer/popover;
- `Synced` remains status only;
- if assistant/notification backend is future work, show honest polished placeholders with their own titles; do not make all icons open the same screen;
- Escape closes the active surface;
- accessible labels match actual destination.

Required tests must click each icon and prove distinct surfaces/titles.

## R11.6 Move footer out of sidebar and center it globally

Remove the old sidebar-bottom Akilta footer block.

At canonical desktop size render a compact global footer near the bottom center of the main application/workspace:

`Built with ♥ for maximum productivity by Akilta`

Requirements:

- heart `♥` is visibly red;
- use canonical Akilta logo/wordmark where practical and ensure it is visible on dark background;
- footer is centered globally/main-workspace, not left trapped inside sidebar;
- it must not overlap cards or create page scroll;
- preserve `Local foundation online` / version information in the sidebar if useful, but separate it from the global Akilta footer.

## R11.7 New project visibility

After a project is registered on Projects page, navigate back to Command Center and prove the project appears in the main Projects rail immediately, without restarting the app.

This is a manual-QA acceptance item in addition to automated R09 tests.

---

# R12 - MAJOR - Project-page truthfulness and transition quality

The Projects page is already connected to the real Project Registry. Preserve that.

Required improvements around it:

- after successful Add Project, shared registry state is refreshed;
- Project card Open cockpit opens exactly that record's ID;
- do not show a fixture preview while the real record is loading;
- no 20-second identity swap or any visible cross-project identity swap;
- registered project card count and sidebar/main rail update consistently;
- stale async response from a previous route must not overwrite a newer project selection.

Do not delete or modify user project folders during these UI operations.

---

# R13 - Tracker / governance / log truthfulness

Update `H!veAI/TASKS.md` additively and truthfully:

- M07.04 strict audit = FAIL;
- M07.05 active until independent audit;
- new manual-QA UI/project-identity findings are part of M07.05;
- M08 unstarted;
- user visual acceptance pending;
- M01 actual in-app restart acceptance remains separately manual/unverified unless genuinely tested through the in-app restart command.

Create additive remediation doc:

`H!veAI/docs/migration/M07.05_BOUNDED_CORRECTNESS_AND_EVIDENCE_REMEDIATION.md`

Do not edit M00-M07.04 Codex logs.

`H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md` is authoritative and already includes these durable latest manual-QA rules. Do not weaken it.

---

# R14 - Full automated regression matrix

Frontend from `H!veAI`:

```powershell
npm run typecheck
npm test -- --reporter=dot
npm run build
npm audit --audit-level=high
```

Rust/Tauri from Git root:

```powershell
cargo fmt --manifest-path H!veAI/src-tauri/Cargo.toml -- --check
cargo check --manifest-path H!veAI/src-tauri/Cargo.toml
cargo test --manifest-path H!veAI/src-tauri/Cargo.toml
cargo build --manifest-path H!veAI/src-tauri/Cargo.toml
```

Do not report only aggregate counts. In the M07.05 log list exact focused test names grouped by R01-R12 and results.

Then run the hardened real publisher:

```powershell
powershell -ExecutionPolicy Bypass -File H!veAI\scripts\publish-dev-qa.ps1
```

Record:

- production no-bundle build success;
- dedicated frontend-ready marker;
- 5173 listener count = 0;
- 8765 listener count = 0;
- no visible console host;
- candidate cleanup;
- stable-path smoke success;
- Desktop `H!veAI.lnk` direct target/icon validation;
- no installer.

---

# R15 - Required real Desktop manual-QA checklist

After automated tests pass and the stable EXE is republished, launch **the real Desktop `H!veAI.lnk`**.

Do not claim final visual PASS automatically. Record screenshots/observations where practical and mark final UI acceptance `PENDING USER VISUAL ACCEPTANCE` until the user personally confirms.

Verify at the user's maximized desktop and, if practical, `1536x1024`:

1. `Global Overview` visible with no visible `WORKSPACE OVERVIEW` label;
2. sidebar is slightly narrower but logos/navigation remain intact;
3. Current Project identity is on a compact row and matches a real registered project;
4. newly registered project appears in Command Center Projects rail without restart;
5. sidebar shortcuts reflect real registered projects rather than hardcoded fixture IDs;
6. opening AI-Commerce-HQ cockpit never displays FormuLab at any point;
7. delayed registry/Git loading cannot change one project's cockpit into another;
8. Current Task has no visible nested scrollbar;
9. Workflow Status has no visible nested scrollbar;
10. Recent Activity has no visible nested scrollbar;
11. Project Metrics has no visible nested scrollbar;
12. System Status has no visible nested scrollbar;
13. whole Command Center has no outer vertical/horizontal scroll at canonical desktop sizes;
14. Command icon opens Command Palette;
15. Sparkles opens distinct AI Assistant surface;
16. Bell opens distinct Notifications surface;
17. global bottom-center footer is visible;
18. footer heart is red;
19. old Akilta footer is no longer trapped in sidebar;
20. H!veAI shell still launches with no external Edge/Chrome browser for the app itself and no visible console;
21. external-browser preference remains Chrome only when H!veAI intentionally opens an external web URL;
22. shortcut still uses canonical small-logo derived icon.

If any item fails, record it explicitly. Do not call UI accepted.

---

# Acceptance criteria

M07.05 automated closure can be reported ready for independent audit only if ALL automated criteria below are true:

1. normal non-empty event refresh cannot clear required rescan;
2. explicit rescan clears only after watcher/Git/persistence recovery succeeds;
3. DB/memory rescan state agrees;
4. repaired project root replaces old watcher root;
5. required watcher production-path tests exist;
6. legacy rowless non-Git registration cannot repair to Git;
7. complete Registry identity matrix tests pass;
8. historical migration 5 body restored to pre-M07.04 semantics;
9. migration 7 identity remains stable;
10. real v6 -> v7 test proves `project_snapshots.created_at` conversion;
11. SQLite repeated/failure/lock/corruption/pragmas tests pass;
12. Git `diff()` uses deterministic binary metadata;
13. staged added binary returns real path, never `/dev/null`;
14. real H!veAI diff() matrix passes;
15. `HIVEAI_FRONTEND_READY` has one dedicated Rust emission source;
16. readiness command is correctly permissioned;
17. production publisher has no build bypass;
18. rollback records/verifies prior stable SHA-256;
19. safe launcher failure-path harness passes;
20. Command Center Tauri project rail uses live Registry identities;
21. new registered project can refresh into main rail/shortcuts without app restart;
22. Project Cockpit has no `projects[0]`/FormuLab fallback race in Tauri mode;
23. delayed project lookup cannot render an unrelated project identity;
24. topbar Command/Assistant/Notifications actions are distinct;
25. frontend tests cover R09-R12 live identity/routing behavior;
26. full frontend/Rust regression matrix passes;
27. real stable publication smoke passes;
28. canonical single-viewport composition remains intact;
29. M08 remains unstarted;
30. no installer exists from this milestone;
31. M07.05 log is committed/pushed and remote-visible.

Manual visual items in R15 remain user-gated.

If any automated BLOCKER/MAJOR remains:

`M07.05 RESULT = FAIL`

If all automated gates close but user visual acceptance remains:

`M07.05 RESULT = PENDING USER VISUAL ACCEPTANCE`

Do not call M08 ready yourself. Independent ChatGPT strict audit decides normal milestone authorization.

---

# Commit / push rules

Use focused commits if practical.

Push normally to `origin/H!veAI`.

Never force push.

After push verify:

- final remote HEAD;
- M07.05 log remote-visible;
- M07.04 strict audit remote-visible;
- this updated M07.05 prompt remote-visible;
- updated `UI_LAYOUT_GOVERNANCE.md` remote-visible;
- historical Codex logs unchanged;
- M08 prompt/code absent.

---

# Final response format

Return these sections:

1. M07.05 RESULT
2. FETCH / SYNC / BRANCH / FINAL HEAD
3. R01 RESCAN CORRECTNESS
4. R02 WATCHER REATTACHMENT + EVIDENCE
5. R03 LEGACY REGISTRY IDENTITY + FULL MATRIX
6. R04 MIGRATION V7 + IMMUTABILITY
7. R05 SQLITE DURABILITY MATRIX
8. R06 GIT DIFF PRODUCT-BOUNDARY MATRIX
9. R07 FRONTEND READINESS SIGNAL
10. R08 LAUNCHER HASH / ROLLBACK MATRIX
11. R09 LIVE PROJECT REGISTRY UI SOURCE OF TRUTH
12. R10 PROJECT COCKPIT ROUTE IDENTITY / NO FORMULAB FALLBACK
13. R11 COMMAND CENTER MANUAL-QA VISUAL FIXES
14. R12 PROJECT PAGE / ASYNC TRANSITION CORRECTNESS
15. R13 TRACKER / GOVERNANCE / LOG INTEGRITY
16. R14 FULL REGRESSION MATRIX
17. R15 DESKTOP MANUAL-QA CHECKLIST STATUS
18. WINDOWS PRODUCTION PUBLICATION
19. PORT 5173 / 8765 / CONSOLE
20. DESKTOP H!veAI.LNK TARGET / ICON
21. INSTALLER STATUS
22. FILES ADDED
23. FILES MODIFIED
24. EXACT FOCUSED TEST NAMES + RESULTS GROUPED BY R01-R12
25. COMMITS / PUSH / REMOTE HEAD
26. REMAINING BLOCKERS / MAJORS
27. MANUAL-ONLY ITEMS
28. EXACT NEXT STEP

Do not collapse focused evidence into a single aggregate test count.
