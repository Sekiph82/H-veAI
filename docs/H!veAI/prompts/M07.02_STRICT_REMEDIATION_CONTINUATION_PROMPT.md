# M07.02 — H!veAI Strict Remediation Continuation

You are continuing H!veAI remediation after the independent M07.01 strict audit returned **FAIL**.

This is a bounded remediation continuation. It is not M08.

Do NOT start M08.
Do NOT create or recommend an M08 prompt.

## Mandatory fetch-before-prompt sync

Before reading this prompt:

```powershell
git fetch origin H!veAI
git rev-list --left-right --count HEAD...origin/H!veAI
```

If local HEAD is behind `origin/H!veAI` and there are no conflicting tracked changes:

```powershell
git merge --ff-only origin/H!veAI
```

Never use reset, destructive checkout, force push, or automatic rebase to force synchronization.

## Canonical locations

Git root:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ`

H!veAI root:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI`

Branch:
`H!veAI`

Canonical product name:
`H!veAI`

## Canonical UI Assets

Canonical UI reference assets are located at:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI`

Use the assets in this folder as authoritative visual references:

- dashboard reference image
- Akilta logo
- H!veAI logo

Do not redesign these assets unless explicitly instructed.

Use the dashboard reference to preserve/reproduce the layout, spacing, visual hierarchy, panels, cards, navigation, right-side assistant/status columns, typography density, and dark visual language as closely as practical for any visible UI touched by this remediation.

Use the H!veAI logo in product branding.
Use the Akilta logo in the footer with exactly:

`Built with ♥ for maximum productivity by Akilta`

This section is mandatory in addition to `H!veAI/AGENTS.md`.

## Read completely before coding

Read:

- `H!veAI/AGENTS.md`
- `H!veAI/CONSTITUTION.md`
- `H!veAI/ARCHITECTURE.md`
- `H!veAI/TASKS.md`
- `H!veAI/CODEX_ROADMAP.md`
- `H!veAI/docs/H!veAI/audits/M07_FILESYSTEM_WATCHER_AND_SNAPSHOTS_STRICT_AUDIT.md`
- `H!veAI/docs/H!veAI/audits/M07.01_CROSS_MILESTONE_REMEDIATION_STRICT_AUDIT.md`
- `H!veAI/docs/H!veAI/prompts/M07.01_CROSS_MILESTONE_REMEDIATION_HARDENING_PROMPT.md`
- `H!veAI/docs/H!veAI/codex-logs/M07.01_CROSS_MILESTONE_REMEDIATION_CODEX_LOG.md`
- historical M00-M07 logs
- this prompt

Treat every prior `PASS`, `COMPLETE`, and `closed` statement as a claim to verify.

## Durable M07.02 log

Create a NEW log before implementation:

`H!veAI/docs/H!veAI/codex-logs/M07.02_STRICT_REMEDIATION_CONTINUATION_CODEX_LOG.md`

Do not edit historical M00-M07 or M07.01 Codex logs.

For every finding below record:

- finding ID
- exact source symbol/file
- previous incorrect behavior
- corrected behavior
- focused tests
- regression evidence
- failures/corrections during implementation
- unresolved/manual items
- final commit/push/remote HEAD

---

# FINDING R01 — BLOCKER — EXPLICIT RESCAN CORRECTNESS

Files/symbols:
- `src-tauri/src/watcher.rs`
- `WatcherManager::rescan_project`
- `refresh_project_snapshot`

Current incorrect behavior:

An explicit rescan can clear `rescan_required` merely because `explicit=true`, including when the registered project root is missing/unavailable. The persisted project snapshot is written before the in-memory clear, so persisted and in-memory rescan state can disagree.

Required behavior:

1. Define an explicit rescan as successful only after the registered project is available and all required reconciliation/evidence refresh steps succeed.
2. Missing/unavailable root MUST NOT count as successful reconciliation.
3. Failed explicit rescan MUST retain `rescan_required=true` when it was already required.
4. Ordinary event refresh must never clear the flag.
5. Persisted project snapshot state and in-memory status must represent the same final rescan state.
6. If explicit rescan fails, return a structured error or explicit degraded result without lying about recovery.
7. A successful explicit rescan may clear the flag only after its evidence persistence succeeds.

Required focused tests:

- overflow -> true
- ordinary refresh -> still true
- successful explicit rescan -> false
- missing-root explicit rescan -> still true
- forced persistence/refresh failure -> still true
- persisted row agrees with in-memory state after both successful and failed rescan

No test may simulate success by directly mutating status after bypassing production reconciliation logic.

---

# FINDING R02 — MAJOR — PHYSICAL WATCHER ROOT CONTAINMENT

Files/symbols:
- `src-tauri/src/watcher.rs`
- `relative_path` / event-path containment helper

Lexical `strip_prefix` is necessary but not sufficient for the explicit symlink/junction escape requirement.

Required behavior:

1. Keep lexical fail-closed rejection.
2. Add physical containment validation suitable for Windows symlink/junction/reparse-point behavior.
3. An event path that lexically appears under the project but resolves through a symlink/junction outside the canonical registered root must not produce a normal project event.
4. Rename with either endpoint outside/escaping scope must fail closed or become a safe `RESCAN_REQUIRED` condition without leaking the outside path.
5. Removed/nonexistent paths must be handled using a safe nearest-existing-ancestor strategy or another documented approach that does not simply disable containment.
6. Frontend-facing normalized events must never contain an outside absolute path.

Required focused tests:

- in-root ordinary path
- sibling/outside path
- `..` path
- positive in-root rename
- rename with outside endpoint
- symlink/junction escape fixture where supported on the running platform
- if Windows fixture creation requires privileges unavailable in the environment, implement the production protection and keep only that specific OS fixture explicitly `UNVERIFIED`; do not omit the protection itself

---

# FINDING R03 — MAJOR — COMPLETE M07 FOCUSED EVIDENCE

Add direct production-path tests for all remaining M07 evidence gaps:

1. positive rename normalization
2. duplicate watcher prevention
3. repaired-path reattachment after Project Registry repair
4. Git-category watcher event -> bounded persisted `git_snapshots` integration
5. non-Git project watcher behavior
6. startup/manager initialization with one missing project remains degraded but succeeds
7. clean manager shutdown joins/releases the worker and watchers
8. IPC boundary has no arbitrary watch-path/file-read command
9. R01 success/failure/persistence cases
10. R02 outside/escape cases

Do not claim these are directly tested unless the focused test exercises the real production path.

---

# FINDING R04 — BLOCKER — DETERMINISTIC BINARY DIFF SAFETY

Files/symbols:
- `src-tauri/src/git_engine/mod.rs`
- `diff`

Removing `--binary` was useful but is not enough for the strict guarantee.

Required behavior:

1. H!veAI `GitDiff.text` MUST never contain raw `GIT binary patch` base85/body content.
2. Detect binary file metadata deterministically.
3. Mixed text+binary diff must retain safe text hunks and list binary paths separately.
4. Add a deterministic sanitization fallback: if raw Git output contains a `GIT binary patch` section for any reason, strip the entire binary payload before `GitDiff.text` is returned.
5. Recalculate truncation after sanitization so the flag reflects the returned text.
6. No binary patch body is persisted.

Preferred architecture:

- use normal textual `git diff --no-ext-diff --no-textconv ...` for safe text
- optionally use a separate bounded metadata command such as `--numstat` to identify binary paths
- never depend solely on the assumption that Git will never emit `GIT binary patch`

Required tests MUST call the real H!veAI `diff()` product boundary through a temporary registered Git project/database, not raw Git alone:

- pure text working-tree diff
- pure binary working-tree diff -> metadata only
- pure binary staged diff -> metadata only
- mixed text + binary working-tree diff
- mixed staged diff where practical
- injected/parser-level `GIT binary patch` fallback sanitation
- truncation after sanitation

Delete or rename misleading tests that claim metadata-only behavior while testing raw `git diff --binary` instead of H!veAI `diff()`.

---

# FINDING R05 — MAJOR — TIMESTAMP MIGRATION COMPLETION

Files:
- `src-tauri/src/time.rs`
- `src-tauri/src/db/migrations.rs`
- Registry/Git/Watcher persistence paths

Current issue:

Migration 5 does not convert `project_snapshots.created_at`, a known field populated by M07's previous epoch-string timestamp implementation.

Required behavior:

1. Do NOT rely on editing migration 5 as the only fix, because the user's local database may already be at schema version 5.
2. Add a new versioned migration, version 6, for residual timestamp standardization/backward compatibility.
3. At minimum convert all known M05-M07 epoch-text timestamp fields that can already contain numeric epoch strings, including `project_snapshots.created_at`.
4. Preserve valid RFC3339 strings.
5. Preserve malformed legacy strings deterministically rather than destroying data, while documenting how they are surfaced.
6. New timestamps remain UTC RFC3339 with millisecond precision.

Required tests:

- timestamp generator exact parseable RFC3339 UTC form
- lexicographic ordering for generated canonical UTC strings
- simulate DB through migration 5 with legacy numeric `project_snapshots.created_at`, then apply migration 6 and prove conversion
- conversion of Registry/Git/Watcher legacy numeric values
- valid RFC3339 preserved
- malformed legacy string behavior
- fresh DB reaches schema v6
- rerun idempotence/history remains correct

---

# FINDING R06 — BLOCKER — PROJECT REPAIR IDENTITY DECISION MATRIX

Files/symbols:
- `src-tauri/src/projects/registry.rs`
- `repair_project_path`
- `insert_repository`
- project detection helpers as needed

Current defects:

1. Non-Git registration creates no `repositories` row. During repair `existing_identity=None`, so a non-Git project can silently repair to an unrelated Git repository.
2. Git projects with the same strong sanitized remote identity are rejected solely because HEAD advanced, making legitimate moved/continued repositories fail repair.
3. Required repair-identity focused tests are absent.

Required decision matrix:

### Registered non-Git

- repair to non-Git path: allowed if duplicate/path rules pass
- repair to Git path: reject by default as identity-type change; do not silently convert

### Registered Git with strong remote identity

- same normalized/sanitized remote identity: repository identity may be accepted even if HEAD advanced
- different remote identity: reject
- remote disappears: reject unless another strong deterministic identity rule proves continuity

### Registered Git without usable remote

Use read-only repository evidence such as stored old HEAD and ancestry/history in the replacement repository:

- exact old HEAD exists/equal: accept
- old HEAD is an ancestor of replacement HEAD: may accept as same history
- unrelated/missing evidence: reject fail closed

Do not mutate the external repository while checking identity.

Required tests:

- same non-Git moved path accepted
- non-Git -> Git rejected
- same Git repo moved path, same remote and same HEAD accepted
- same Git repo, same remote but advanced HEAD accepted
- different remote rejected
- remote disappears rejected unless explicit history evidence satisfies policy
- no-remote repo with old HEAD present/ancestor accepted
- unrelated no-remote repo rejected
- credential-bearing remote variants sanitize to same identity
- duplicate repair path rejected
- external repo contents/metadata not mutated

Document the matrix in migration/remediation docs.

---

# FINDING R07 — MAJOR — SQLITE WAL-CONSISTENT BACKUP / LOCK / CORRUPTION

Files:
- `src-tauri/src/db/mod.rs`
- `src-tauri/Cargo.toml` if a rusqlite feature is required

Current issue:

Raw `fs::copy` of only `hiveai.db` is not a sufficient SQLite-consistent backup strategy while WAL mode can contain committed pages outside the main database file.

Required behavior:

1. Replace raw main-file backup with a SQLite-aware consistent backup mechanism, preferably `rusqlite` backup API or another documented SQLite-consistent method.
2. Backup must represent the logical database including committed WAL content.
3. Use a temp/staging destination and publish only after the SQLite backup completes successfully.
4. Define bounded retention. Avoid an overwrite scheme that becomes fragile when a prior backup already exists.
5. Backup failure must abort migration before schema mutation.
6. Corrupt DB must never be silently overwritten/reinitialized.
7. Lock/busy behavior must remain bounded and observable.

Required tests:

- WAL-mode committed data is present in the produced backup
- existing prior backup/retention path does not break the next migration backup
- backup failure leaves source DB unchanged
- locked/busy write returns predictably within bounded timeout
- corrupted DB initialization fails without overwriting source bytes
- quick-check healthy DB succeeds
- every application connection still receives FK/WAL/busy/synchronous policy

Do not introduce a second persistence stack.

---

# FINDING R08 — MAJOR — COMPLETE REGRESSION EVIDENCE

After all focused fixes, run and record every required verification explicitly.

Frontend from `H!veAI`:

```powershell
npm run typecheck
npm test -- --reporter=dot
npm run build
npm audit --audit-level=high
```

Rust/Tauri:

```powershell
cargo fmt --manifest-path H!veAI/src-tauri/Cargo.toml -- --check
cargo check --manifest-path H!veAI/src-tauri/Cargo.toml
cargo test --manifest-path H!veAI/src-tauri/Cargo.toml
cargo build --manifest-path H!veAI/src-tauri/Cargo.toml
```

Also perform and record:

- fresh DB migration to v6
- historical v5 -> v6 migration fixture
- Registry identity matrix tests
- Git text/binary/mixed diff matrix
- watcher rename/overflow/rescan/repaired-root/Git-refresh integration
- runtime/database/watcher startup smoke
- no legacy process
- port 8765 listener count = 0
- clean shutdown

A passing aggregate test count without the required focused evidence is not sufficient.

---

# FINDING R09 — MAJOR — ADDITIVE TRUTHFULNESS CORRECTION

Historical Codex logs are immutable.

Do not edit:
- M00-M07 logs
- M07.01 Codex log

Create/update additive remediation documentation that explicitly states which M07.01 closure claims were reopened by the independent strict audit and how M07.02 closes them.

Update `TASKS.md` truthfully:

- M08 stays unstarted
- M07/M07.01 must not be described as strict PASS until independent audit passes
- M01 restart remains blocked/manual unless actual in-app restart evidence is completed
- manual visual items remain pending until the user confirms them

M07.02 log must not say `PASS`, `APPROVED`, or `all blockers closed` unless every required automated blocker/major criterion in this prompt has direct evidence.

---

# FINDING R11 — MINOR BUT REQUIRED HERE — DURABLE DEVELOPMENT QA PUBLICATION

The user wants one stable Desktop shortcut throughout active development and does not want to reinstall after each milestone.

Current reported local target:

`H!veAI\dev-bin\H!veAI.exe`

Desktop shortcut:

`Desktop\H!veAI.lnk`

Add a durable developer-only publication helper under the repository, for example:

`H!veAI/scripts/publish-dev-qa.ps1`

or an equivalent safe script.

This helper is NOT the user launcher. The user launcher remains the `.lnk` directly targeting `H!veAI.exe`.

Helper requirements:

1. receive/use the successfully built real desktop executable
2. stage/copy to a temporary file first
3. validate Windows PE signature (`MZ`) and nonzero file size
4. replace `dev-bin/H!veAI.exe` only after validation succeeds
5. preserve previous validated executable if publication fails
6. verify Desktop `H!veAI.lnk` target resolves to the stable executable
7. never invoke a Windows installer
8. never make `.lnk` depend on `.bat`, cmd, PowerShell, npm, cargo, or Vite

Add a durable `Development Manual QA Launcher` rule to `H!veAI/AGENTS.md`:

- after each future milestone's full verification succeeds, refresh the stable dev executable using the publication helper
- keep the `.lnk` target stable
- do not require setup/reinstallation during development
- final Windows installer remains an M20/final-release concern

Do NOT alter or delete the preserved parent `start-demo.bat`.

---

# MANUAL ACCEPTANCE ITEMS — DO NOT FABRICATE

The following remain human/local acceptance items unless genuinely automated with direct evidence:

1. Desktop `H!veAI.lnk` visually opens the application on the user's desktop
2. visual click-through at 1100x760 against canonical dashboard reference
3. visual click-through at 720x520
4. actual in-app restart-command full cycle

Keep these `UNVERIFIED / MANUAL ACCEPTANCE REQUIRED` if not actually performed.

Do not block automated remediation work on inability to fabricate these results.

M08 remains blocked until the independent audit decides automated closure is sufficient and any explicitly required human gate is handled.

---

# SECURITY / ARCHITECTURE CONSTRAINTS

Preserve:

- Rust-native H!veAI runtime
- no always-on Python sidecar
- one SQLite persistence stack
- registered-project boundaries
- no arbitrary shell IPC
- no arbitrary SQL IPC
- no arbitrary watcher path IPC
- Git writes default denied
- no implicit network fetch/push
- no real user project mutation in tests
- no secret logging
- no installer in M07.02

All filesystem/Git/DB mutation tests use temporary fixtures.

---

# REQUIRED M07.02 ACCEPTANCE CRITERIA

M07.02 can report automated remediation complete only if all of these are satisfied:

1. missing/failed explicit rescan cannot clear required-rescan state
2. successful explicit rescan clears only after persistence/evidence succeeds
3. persisted/in-memory rescan state agree
4. lexical and physical watcher containment policy is implemented
5. all M07 focused evidence gaps have direct tests
6. H!veAI `diff()` deterministically strips/prevents binary patch payloads
7. full text/binary/mixed/staged/working-tree diff tests exercise the product boundary
8. migration v6 completes canonical timestamp conversion including project snapshot creation timestamps
9. required timestamp migration/format/order/malformed tests pass
10. non-Git -> Git repair is rejected
11. same Git identity with advanced HEAD is handled according to documented decision matrix
12. no-remote Git identity uses safe history evidence and fails closed when ambiguous
13. full Project Registry identity matrix tests pass
14. SQLite backup is SQLite/WAL-consistent
15. backup retention/repeated-backup/failure tests pass
16. lock/busy test proves bounded behavior
17. corruption test proves source DB is not overwritten
18. runtime dead-code cleanup remains intact
19. production CSP hardening remains intact
20. complete frontend and Rust regression command matrix is recorded and green
21. legacy process absent and port 8765 has zero listeners in bounded smoke
22. historical logs remain immutable
23. TASKS/remediation docs are truthful
24. durable dev-QA publication helper exists and preserves last good executable on failure
25. AGENTS contains the durable Development Manual QA Launcher rule
26. no installer is introduced
27. M08 remains unstarted
28. M07.02 log is committed/pushed and final remote HEAD verified

Any unresolved automated BLOCKER or MAJOR means `M07.02 RESULT = FAIL`.

---

# COMMIT / PUSH

Use focused commits.

Required new log:

`H!veAI/docs/H!veAI/codex-logs/M07.02_STRICT_REMEDIATION_CONTINUATION_CODEX_LOG.md`

Push normally to `origin/H!veAI`.
Do not force push.

After push verify:

- final remote HEAD
- M07.02 log remote-visible
- M07.01 strict audit remote-visible
- M07.02 prompt remote-visible
- historical logs unchanged
- no M08 prompt/code created

---

# FINAL RESPONSE FORMAT

Return exactly:

1. M07.02 RESULT
2. FETCH / SYNC / BRANCH / FINAL HEAD
3. R01 RESCAN STATE CLOSURE
4. R02 PHYSICAL ROOT CONTAINMENT
5. R03 M07 FOCUSED TEST CLOSURE
6. R04 BINARY DIFF PRODUCT-BOUNDARY CLOSURE
7. R05 TIMESTAMP V6 MIGRATION
8. R06 PROJECT REPAIR IDENTITY MATRIX
9. R07 SQLITE WAL BACKUP / LOCK / CORRUPTION
10. R08 FULL REGRESSION MATRIX
11. R09 TRACKER / DOC TRUTHFULNESS
12. DEVELOPMENT QA PUBLICATION HELPER
13. AGENTS DEVELOPMENT LAUNCHER RULE
14. CSP / RUNTIME REGRESSION
15. DEPENDENCY / SECURITY RESULTS
16. WINDOWS SMOKE / LEGACY PORT 8765
17. DESKTOP H!veAI.LNK LOCAL STATUS
18. VISUAL QA MANUAL STATUS
19. RESTART MANUAL STATUS
20. INSTALLER STATUS — MUST BE NOT IMPLEMENTED
21. FILES ADDED
22. FILES MODIFIED
23. HISTORICAL LOG INTEGRITY
24. M07.02 LOG PATH / GITHUB VERIFICATION
25. COMMITS / PUSH / REMOTE HEAD
26. REMAINING BLOCKERS / MAJORS
27. UNVERIFIED MANUAL ITEMS
28. EXACT NEXT STEP

If any automated blocker/major remains:

`M07.02 — REMEDIATION CONTINUES`

If all automated blocker/major findings are closed but human items remain:

`M07.02 — PENDING HUMAN ACCEPTANCE`

Do not name M08 as ready unless independent ChatGPT audit later authorizes it.
Do not create or recommend an M08 prompt.
Do not start M08.