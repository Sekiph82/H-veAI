# M07.07 — Claude Surgical Remediation

You have not worked on H!veAI before. This is a deliberately narrow remediation pass intended to minimize repository exploration and token use.

Do not start M08. Do not create an installer. Do not redesign the current dashboard. Preserve the current names-only Global Overview project rail, in-place project selection, explicit Open cockpit navigation, dedicated frontend-ready IPC, Git diff fixes, H!veAI branding, Akilta footer, and single-viewport layout.

## Exact locations

Windows Git root:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ`

Windows app root:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI`

GitHub repository:
`https://github.com/Sekiph82/AI-Commerce-HQ`

Branch:
`H!veAI`

Branch URL:
`https://github.com/Sekiph82/AI-Commerce-HQ/tree/H!veAI`

First follow the mandatory fetch-before-prompt rule in:

Windows:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\AGENTS.md`

GitHub:
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/AGENTS.md`

Then read the strict audit you are remediating:

Windows:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\docs\H!veAI\audits\M07.06_FOCUSED_EVIDENCE_AND_RUNTIME_TRUTH_STRICT_AUDIT.md`

GitHub:
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/docs/H!veAI/audits/M07.06_FOCUSED_EVIDENCE_AND_RUNTIME_TRUTH_STRICT_AUDIT.md`

Read the M07.06 Codex log only as claims, not proof:

Windows:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\docs\H!veAI\codex-logs\M07.06_FOCUSED_EVIDENCE_AND_RUNTIME_TRUTH_CLOSURE_CODEX_LOG.md`

GitHub:
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/docs/H!veAI/codex-logs/M07.06_FOCUSED_EVIDENCE_AND_RUNTIME_TRUTH_CLOSURE_CODEX_LOG.md`

## Scope: inspect and modify only these files unless a directly imported helper is strictly required

SQLite:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\src-tauri\src\db\mod.rs`
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/src-tauri/src/db/mod.rs`

Watcher:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\src-tauri\src\watcher.rs`
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/src-tauri/src/watcher.rs`

Registry:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\src-tauri\src\projects\registry.rs`
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/src-tauri/src/projects/registry.rs`

Publisher:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\scripts\publish-dev-qa.ps1`
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/scripts/publish-dev-qa.ps1`

Publisher test harness:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\scripts\tests\publish-dev-qa-failure-harness.ps1`
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/scripts/tests/publish-dev-qa-failure-harness.ps1`

Frontend focused tests:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\tests\m07.06-focused.test.tsx`
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/tests/m07.06-focused.test.tsx`

Legacy frontend regression tests only if required for current schema expectations:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\tests\app.test.tsx`
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/tests/app.test.tsx`

Tracker:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\TASKS.md`
`https://github.com/Sekiph82/AI-Commerce-HQ/blob/H!veAI/H!veAI/TASKS.md`

## 1. SQLite corruption-safe preflight

Current defect: `DatabaseState::initialize()` configures the normal connection, including WAL mode, before running `PRAGMA quick_check(1)`. The strict contract requires an existing corrupt database to fail integrity preflight before write-affecting configuration/migration work can occur.

Implement a bounded read-only integrity preflight for an existing non-empty DB before normal connection configuration. Preserve fresh DB behavior and logical pre-migration backup behavior.

Add direct temp-only tests named or equivalently scoped to prove:

- corrupt DB preflight fails without changing original source bytes;
- busy/locked contention respects configured bounds;
- backup publication failure preserves source and prior good backup;
- backup failure prevents migration schema mutation;
- every normal `open_connection()` reports FK, WAL, busy timeout and synchronous policy.

Private `cfg(test)` injection is acceptable. Do not add a public bypass.

## 2. Complete watcher evidence matrix

Do not rewrite production watcher logic unless a direct test exposes a defect.

Add focused temp-only tests for:

- attachment failure preserves rescan requirement;
- Git refresh failure preserves rescan requirement;
- snapshot persistence failure preserves rescan requirement;
- healthy explicit rescan with no prior requirement is deterministic;
- positive in-root rename preserves old/new relative paths;
- unchanged refresh does not duplicate a watcher;
- repaired root refresh reattaches to new root;
- event on repaired root is observed;
- Git-category event persists a Git snapshot;
- non-Git event does not persist a Git snapshot;
- initialization with missing registered root stays degraded and preserves Registry row;
- manager drop releases worker/watchers;
- physical containment rejects symlink/junction escape where the test environment supports it.

If a Windows link/junction case genuinely cannot be automated in the environment, mark only that case UNVERIFIED with exact reason rather than inventing a pass.

## 3. Complete Registry identity tests

Use real `repair_project_path()` calls and temp repositories. Cover all of these directly:

1. legacy rowless non-Git -> moved non-Git accepted;
2. legacy rowless non-Git -> Git rejected;
3. explicit non-Git -> Git rejected;
4. same remote/same HEAD accepted;
5. same remote/advanced HEAD accepted;
6. different remote rejected;
7. remote disappearance rejected;
8. no-remote exact HEAD accepted;
9. no-remote ancestor accepted;
10. unrelated no-remote rejected;
11. missing stored old HEAD rejected;
12. credential-bearing equivalent HTTPS remote sanitizes to the same identity;
13. duplicate normalized repair target rejected;
14. unknown project ID rejected;
15. repair validation leaves candidate repository evidence unchanged.

Do not add network fetch/pull/reset/checkout behavior to validation.

## 4. Complete frontend live-Registry / route-race tests

Do not redesign UI. Add direct Tauri-mode tests proving:

- newly registered project refresh appears in Command Center rail;
- sidebar shortcuts derive from live Registry IDs;
- archive/remove refresh removes project from rail and shortcuts;
- pending project lookup renders loading without unrelated identity;
- delayed AI-Commerce-HQ lookup never flashes FormuLab;
- stale earlier route lookup cannot overwrite newer route;
- unknown registered ID renders error/not-found, never FormuLab;
- Git-status async completion cannot change the project identity;
- browser-preview fixtures remain isolated from Tauri live Registry mode.

Important mock rule: unknown `hiveai_project_get` IDs must fail/not-found. Never fall back to `records[0]` in the mock.

If needed, update stale schema-v4 expectations in `app.test.tsx` to schema v7 without rewriting unrelated M02 coverage.

## 5. Finish launcher failure evidence

Preserve the production publisher's current no-bypass interface and SHA-256 rollback behavior.

Extend the temp-only harness so the evidence map explicitly covers:

- invalid candidate leaves stable unchanged;
- simulated build/provenance failure leaves stable unchanged;
- readiness failure leaves stable unchanged;
- shortcut/icon precondition failure leaves stable unchanged;
- simulated post-swap failure restores exact prior SHA-256;
- locked stable fails cleanly without byte change;
- failed smoke path leaves no spawned test process;
- successful temp swap final hash equals candidate;
- production publisher exposes no SkipBuild/external-candidate bypass.

After all project tests pass, run the normal real publisher once and record that result separately. The temp harness must never address the real Desktop shortcut or real stable executable.

## 6. Log and tracker

Create a new log before editing:

Windows:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI\docs\H!veAI\codex-logs\M07.07_CLAUDE_SURGICAL_REMEDIATION_LOG.md`

Repository path:
`H!veAI/docs/H!veAI/codex-logs/M07.07_CLAUDE_SURGICAL_REMEDIATION_LOG.md`

The log must contain exact modified files, exact focused test names, which tests hit production functions versus isolated simulation, commands/results, any UNVERIFIED item, and final local/remote HEAD.

Update `TASKS.md` truthfully: M07.06 strict audit = FAIL, M07.07 active until independent audit, M08 unstarted. Place M07.07 with the M07 remediation sequence, not after M20.

Historical M00-M07.06 logs are immutable.

## Verification

Run focused tests first for DB, watcher, Registry and the M07.06 frontend focused suite, then the existing full frontend/Rust regression commands required by `AGENTS.md`. Run the isolated publisher harness. Only if all automated gates pass, run the normal `H!veAI\scripts\publish-dev-qa.ps1` once to republish the stable QA executable.

Final status must be:

- `M07.07 RESULT = FAIL` if any automated MAJOR remains, or
- `M07.07 RESULT = PENDING USER VISUAL + M01 RESTART ACCEPTANCE` if automated closure succeeds.

Stop there. Do not start M08.