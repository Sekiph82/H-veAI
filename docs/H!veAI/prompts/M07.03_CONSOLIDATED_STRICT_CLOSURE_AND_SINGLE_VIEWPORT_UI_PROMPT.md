# M07.03 — H!veAI Consolidated Strict Closure + Single-Viewport UI Fidelity

You are continuing H!veAI after the independent strict audit of M07.02A returned **FAIL**.

This prompt intentionally consolidates:

- every still-open automated requirement from `M07.02_STRICT_REMEDIATION_CONTINUATION_PROMPT.md`;
- the M07.02A durable-launcher defects found by independent audit;
- the complete single-viewport/dashboard-fidelity requirements that existed in `M07.02B_SINGLE_VIEWPORT_DASHBOARD_FIDELITY_HOTFIX.md` but were **not executed by Codex**;
- direct user manual-QA screenshots showing the current Command Center still diverges materially from the canonical dashboard reference.

This is a strict quality gate. It is not M08.

Do NOT start M08.
Do NOT create or recommend an M08 prompt.

---

## Mandatory fetch-before-prompt sync

From the Git root:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ`

run:

```powershell
git fetch origin H!veAI
git rev-list --left-right --count HEAD...origin/H!veAI
```

If local HEAD is safely behind with no conflicting tracked changes:

```powershell
git merge --ff-only origin/H!veAI
```

Never use reset, destructive checkout, force push, or automatic rebase.

---

## Canonical roots / product identity

Git root:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ`

H!veAI root:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI`

Branch:

`H!veAI`

Canonical product name:

`H!veAI`

---

# Canonical UI Assets

Canonical UI reference assets are located at:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI`

Use these assets as authoritative visual references:

- dashboard reference image
- Akilta logo
- H!veAI logo
- `H!veAI small logo.png`
- `H!veAI text logo.png`

Do not redesign, recolor, crop, stretch, replace, or reinterpret these assets unless explicitly instructed by the user.

Canonical shortcut/small emblem source:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI\H!veAI small logo.png`

Canonical sidebar wordmark source:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI\H!veAI text logo.png`

Use the Akilta logo with the footer text exactly:

`Built with ♥ for maximum productivity by Akilta`

This section is mandatory in addition to `H!veAI/AGENTS.md` and `H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`.

---

## Read completely before coding

Read:

- `H!veAI/AGENTS.md`
- `H!veAI/CONSTITUTION.md`
- `H!veAI/ARCHITECTURE.md`
- `H!veAI/TASKS.md`
- `H!veAI/CODEX_ROADMAP.md`
- `H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`
- `H!veAI/docs/H!veAI/audits/M07_FILESYSTEM_WATCHER_AND_SNAPSHOTS_STRICT_AUDIT.md`
- `H!veAI/docs/H!veAI/audits/M07.01_CROSS_MILESTONE_REMEDIATION_STRICT_AUDIT.md`
- `H!veAI/docs/H!veAI/audits/M07.02A_DEV_QA_LAUNCHER_STRICT_AUDIT.md`
- `H!veAI/docs/H!veAI/prompts/M07.02_STRICT_REMEDIATION_CONTINUATION_PROMPT.md`
- `H!veAI/docs/H!veAI/prompts/M07.02A_DEV_QA_LAUNCHER_BUILD_MODE_HOTFIX.md`
- `H!veAI/docs/H!veAI/prompts/M07.02B_SINGLE_VIEWPORT_DASHBOARD_FIDELITY_HOTFIX.md`
- `H!veAI/docs/H!veAI/codex-logs/M07.02_STRICT_REMEDIATION_CONTINUATION_CODEX_LOG.md`
- historical M00-M07 and M07.01 logs
- this prompt

Treat every historical `PASS`, `COMPLETE`, and `closed` claim as a hypothesis to verify.

The M07.02B file is historical requirements evidence. Do not execute it as a separate session. Its complete required behavior is incorporated below.

---

## Durable M07.03 log

Create a NEW log before implementation:

`H!veAI/docs/H!veAI/codex-logs/M07.03_CONSOLIDATED_STRICT_CLOSURE_CODEX_LOG.md`

Do not edit historical logs.

For every finding below record:

- finding ID
- exact files/symbols
- previous incorrect behavior
- corrected behavior
- focused tests/evidence
- regression evidence
- failures/corrections during implementation
- manual/unverified items
- commits/push/final remote HEAD

---

# PART A — COMPLETE ALL STILL-OPEN M07.02 AUTOMATED FINDINGS

The prior M07.02A log covered only the launcher hotfix. It did **not** close the broader original M07.02 gate. Complete the remaining original strict-remediation requirements now.

## A01 — BLOCKER — Explicit rescan correctness

Files/symbols include:

- `src-tauri/src/watcher.rs`
- `WatcherManager::rescan_project`
- snapshot refresh/persistence logic

Required behavior:

1. Ordinary refresh never clears `rescan_required`.
2. Explicit rescan clears it only after the registered project is available and all required reconciliation/evidence persistence succeeds.
3. Missing/unavailable project root is not a successful rescan.
4. Failed explicit rescan retains `rescan_required=true` when recovery was required.
5. Persisted project snapshot and in-memory watcher status agree after success and failure.
6. Do not claim recovery before persistence succeeds.

Required direct production-path tests:

- overflow -> true
- ordinary refresh -> still true
- successful explicit rescan -> false
- missing-root explicit rescan -> still true
- forced refresh/persistence failure -> still true
- persisted/in-memory equality after both success and failure

## A02 — MAJOR — Physical watcher root containment

Keep lexical `strip_prefix` fail-closed behavior and implement physical/canonical containment against symlink/junction/reparse-point escapes.

Requirements:

- in-root paths accepted;
- sibling/outside paths rejected;
- traversal rejected;
- path that lexically sits under root but resolves outside through symlink/junction must not generate a normal event;
- rename with either endpoint outside/escaping fails closed or produces a safe rescan-required signal without leaking the outside path;
- removed/nonexistent paths use a documented safe nearest-existing-ancestor or equivalent strategy;
- frontend events never expose an outside absolute path.

Direct tests:

- in-root path
- sibling/outside path
- traversal path
- positive in-root rename
- outside-endpoint rename
- symlink/junction escape fixture where platform privileges permit

If the specific Windows fixture cannot be created due OS privilege, keep only that fixture `UNVERIFIED`, not the production protection itself.

## A03 — MAJOR — Complete M07 focused evidence

Add real production-path tests for:

1. positive rename normalization
2. duplicate watcher prevention
3. repaired-path reattachment after Registry repair
4. Git-category watcher event -> bounded persisted `git_snapshots` integration
5. non-Git watcher behavior
6. startup with one missing project remains degraded but manager initializes
7. clean watcher manager shutdown releases/joins worker/watchers
8. IPC has no arbitrary watch-path/file-read command
9. A01 cases
10. A02 cases

Do not count mocks/helpers that bypass production paths.

## A04 — BLOCKER — Deterministic binary diff safety

Files/symbols:

- `src-tauri/src/git_engine/mod.rs`
- `diff`

Required behavior:

1. `GitDiff.text` never returns raw `GIT binary patch` body/base85 payload.
2. Binary files are metadata-only.
3. Mixed text+binary preserves safe text hunks while binary paths are separately listed.
4. Add deterministic sanitizer fallback that strips entire `GIT binary patch` payload sections if Git emits them for any reason.
5. Truncation is calculated after sanitation against returned text.
6. No binary patch body is persisted.

Preferred safe approach:

- textual diff with `--no-ext-diff --no-textconv`;
- bounded metadata command such as `--numstat` for binary detection;
- sanitizer fallback independent of assumptions about Git output mode.

Required tests must call the actual H!veAI `diff()` boundary through temporary registered Git projects/databases:

- text working-tree diff
- binary working-tree metadata-only
- binary staged metadata-only
- mixed working-tree
- mixed staged where practical
- injected/parser-level binary-patch sanitizer
- post-sanitation truncation

Delete/rename misleading tests that only inspect raw `git diff --binary` while claiming H!veAI metadata-only behavior.

## A05 — MAJOR — Timestamp migration v6 completion

Do not rewrite migration 5 as the only remedy.

Add migration v6 to cover residual legacy epoch-text fields, including at minimum:

- `project_snapshots.created_at`
- all other known M05-M07 timestamp columns that could contain numeric epoch strings

Requirements:

- canonical new timestamps are UTC RFC3339 with millisecond precision;
- valid RFC3339 preserved;
- malformed legacy values preserved deterministically and documented;
- fresh DB reaches v6;
- v5 -> v6 historical fixture converts legacy values safely;
- migration history remains idempotent/inspectable.

Tests:

- exact parseable RFC3339 UTC generator shape
- lexical chronological ordering for canonical UTC strings
- v5 -> v6 conversion of `project_snapshots.created_at`
- Registry/Git/Watcher legacy numeric conversions
- valid RFC3339 preservation
- malformed-value behavior
- fresh v6
- rerun idempotence

## A06 — BLOCKER — Project repair identity decision matrix

Files/symbols:

- `src-tauri/src/projects/registry.rs`
- `repair_project_path`
- repository detection/identity helpers

Required decision matrix:

### Registered non-Git

- non-Git -> non-Git repair may succeed if path/duplicate rules pass;
- non-Git -> Git must reject by default as identity-type change.

### Registered Git with strong sanitized remote identity

- same normalized/sanitized remote may be accepted even if HEAD advanced;
- different remote rejects;
- disappearing remote rejects unless another strong deterministic continuity rule proves identity.

### Registered Git without usable remote

Use read-only history evidence:

- exact stored old HEAD exists/equal -> accept;
- stored old HEAD is ancestor of new HEAD -> may accept;
- unrelated/missing history evidence -> reject fail closed.

Never mutate external repositories while checking identity.

Direct tests:

- non-Git moved to non-Git accepted
- non-Git -> Git rejected
- same remote + same HEAD accepted
- same remote + advanced HEAD accepted
- different remote rejected
- remote disappearance policy
- no-remote exact/ancestor continuity accepted
- unrelated no-remote rejected
- credential-bearing remote variants normalize to same identity
- duplicate repair path rejected
- external repo untouched

## A07 — MAJOR — SQLite WAL-consistent backup / lock / corruption

Files include:

- `src-tauri/src/db/mod.rs`
- `src-tauri/Cargo.toml` if rusqlite backup feature is required

Replace raw main-file `fs::copy` backup with SQLite-aware/WAL-consistent logical backup.

Requirements:

1. backup includes committed WAL content;
2. use temp/staging destination and publish only after backup completes;
3. define bounded retention/repeated-backup behavior;
4. backup failure aborts migration before schema mutation;
5. corrupt DB is never silently overwritten/reinitialized;
6. lock/busy behavior is bounded and observable;
7. all app connections keep FK/WAL/busy/synchronous policy.

Required tests:

- committed WAL data exists in backup
- repeated/prior backup does not break next backup
- backup failure leaves source unchanged
- locked/busy write fails predictably within configured bound
- corrupted DB initialization fails without overwriting source bytes
- healthy quick-check succeeds
- every connection gets safety pragmas

Do not introduce a second persistence stack.

## A08 — MAJOR — Complete regression evidence

After focused fixes, explicitly run and record:

Frontend from `H!veAI`:

```powershell
npm run typecheck
npm test -- --reporter=dot
npm run build
npm audit --audit-level=high
```

Rust/Tauri:

```powershell
cargo fmt --manifest-path H!veAI/src-tauri/Cargo.toml -- --check
cargo check --manifest-path H!veAI/src-tauri/Cargo.toml
cargo test --manifest-path H!veAI/src-tauri/Cargo.toml
cargo build --manifest-path H!veAI/src-tauri/Cargo.toml
```

Also record focused integration evidence for:

- fresh DB -> v6
- historical v5 -> v6
- Registry identity matrix
- Git text/binary/mixed matrix
- watcher rename/overflow/rescan/repaired-root/Git-refresh
- runtime/database/watcher startup
- no legacy process
- port 8765 listener count = 0
- clean shutdown

Aggregate test count alone is insufficient.

## A09 — MAJOR — Tracker/docs/log truthfulness

Historical logs remain immutable.

Update additive remediation docs and `TASKS.md` truthfully:

- M08 unstarted;
- M07/M07.01/M07.02 are not strict PASS unless independent audit passes;
- M01 restart remains manual/blocked unless genuine in-app restart evidence exists;
- visual acceptance remains pending until user review;
- do not describe unverified items as closed.

---

# PART B — M07.02A DURABLE LAUNCHER REMEDIATION

The currently published executable works in manual QA, but the independent M07.02A audit found the helper itself unsafe/incomplete.

## B01 — BLOCKER — Enforce Tauri production-build provenance

Current bad durability behavior:

`publish-dev-qa.ps1` accepts `src-tauri\target\release\hiveai-desktop.exe` and validates only the Windows PE `MZ` header. The same path can be produced by raw Cargo, so the helper cannot prove the artifact is a Tauri production-mode build with embedded `frontendDist`.

Required durable behavior:

Preferred architecture: make the publication helper orchestrate the correct build itself.

From the H!veAI workspace, it should run or require proof of:

```powershell
npm run tauri:build -- --no-bundle
```

Then use the resulting candidate.

If you choose another architecture, it must provide equally strong deterministic provenance and must not accept an arbitrary PE as sufficient proof.

Never publish:

- `tauri dev` artifact
- arbitrary raw Cargo artifact
- any executable still dependent on `build.devUrl` / 5173

## B02 — BLOCKER — Candidate smoke BEFORE stable replacement

Before touching `dev-bin\H!veAI.exe`:

1. prove port 5173 is not listening;
2. stage the production-mode candidate;
3. validate nonzero PE;
4. launch the staged candidate with Vite stopped;
5. prove H!veAI native window appears;
6. prove no `127.0.0.1` / connection-refused dependency;
7. prove no separate console/conhost is created for the normal release GUI path;
8. prove no port 8765 / legacy sidecar;
9. close staged candidate cleanly.

Only then is the candidate eligible for stable publication.

## B03 — MAJOR — Transactional last-known-good replacement / rollback

The current helper replaces stable EXE before validating shortcut/icon and has no rollback.

Required behavior:

- validate shortcut target/icon preconditions before stable swap where possible;
- keep the prior stable executable as a temporary rollback copy or use an atomic replace strategy with recoverable old version;
- publish new executable only after candidate smoke succeeds;
- after publication run bounded post-publication checks;
- if any post-publication check fails, automatically restore the previous known-good executable byte-for-byte;
- clean temporary staging/rollback artifacts after success;
- if stable executable is currently running/locked, fail clearly without damaging last-known-good state.

Required failure-path tests or controlled script tests:

- invalid PE -> stable hash unchanged
- wrong/non-production candidate or failed smoke -> stable hash unchanged
- bad shortcut/icon validation -> stable restored/unchanged
- simulated post-publication failure -> prior stable restored
- successful publication -> stable hash equals validated candidate

## B04 — Durable AGENTS launcher governance

Update `H!veAI/AGENTS.md` so future milestones explicitly require:

- stable manual QA binary comes from Tauri production mode / no-bundle build;
- Vite/5173 absent during candidate smoke;
- candidate smoke before stable replacement;
- no visible console for normal release GUI launch;
- last-known-good rollback-safe publication;
- shortcut stays direct to `dev-bin\H!veAI.exe`;
- canonical small-logo-derived ICO;
- no installer during active development;
- final installer remains final-release/M20 concern.

Do not remove existing launcher/browser/canonical asset governance.

---

# PART C — SINGLE-VIEWPORT COMMAND CENTER / CANONICAL DASHBOARD FIDELITY

This section incorporates the complete previously-unexecuted M07.02B visual hotfix.

Direct user screenshots are authoritative evidence that the current Command Center is wrong:

- user needed about four vertical scrolls to reach the page end;
- current layout is a vertically stacked report rather than the canonical command-center console;
- current top-left logo composition does not fit correctly;
- current layout is materially different from the supplied canonical dashboard reference.

These are blocking acceptance defects.

## C01 — BLOCKER — One desktop viewport, no outer scroll

At both desktop QA sizes:

- `1536x1024`
- `2048x1280`

required:

1. complete primary Command Center overview visible within one application viewport;
2. no outer/body vertical scrollbar;
3. no outer/body horizontal scrollbar;
4. do not fake compliance using browser zoom or global CSS transform scaling;
5. use real viewport-height grid/flex composition;
6. nested grid/flex children use `min-height:0`/`min-width:0` as necessary;
7. large lists may scroll internally inside bounded panels;
8. other routes retain route-appropriate scrolling.

Preferred shell:

```text
App viewport
├─ primary sidebar, full height
└─ main
   ├─ compact topbar
   └─ Command Center viewport grid, remaining height
```

## C02 — BLOCKER — Canonical dashboard composition

Reproduce the supplied reference as closely as practical using current available fixture/live data without prematurely implementing future M11/M12 business logic.

Required desktop composition:

1. far-left primary navigation sidebar;
2. compact top/global overview header;
3. compact KPI/status strip;
4. secondary Projects rail/list inside the dashboard workspace;
5. dominant central Project Cockpit/current-project surface;
6. right vertical rail with:
   - AI Engineering Brief
   - AI Assistant
   - System Status
7. compact Recent Activity integrated into the center/bottom area;
8. compact Project Metrics integrated into center/bottom;
9. compact footer branding.

Do NOT retain the current structure where:

- large Project Operation cards dominate a 2-column card grid;
- Needs Your Attention forms a long separate column;
- Runtime/Database/Watcher are three full-width rows;
- Active Work Queue/Recent Activity continue as page-length full-width sections below;
- giant header/empty gaps consume viewport height.

Use existing data where possible but visually recompose it to the canonical dashboard structure.

## C03 — BLOCKER — Compact infrastructure status

Do not delete Runtime, Database, Filesystem Watcher, Git, or related health functionality.

On the primary Command Center:

- remove giant full-width infrastructure blocks from the main vertical flow;
- show compact key health states in right-side `System Status` matching the reference family;
- preserve detailed access elsewhere/expandably without scope creep;
- keep all underlying IPC/tests intact.

## C04 — BLOCKER — Top-left branding exactly as requested

Use:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI\H!veAI small logo.png`

for the small square/emblem position.

Use:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI\H!veAI text logo.png`

for the wordmark immediately beside it.

Target composition:

```text
[ small H!veAI emblem ] [ H!veAI text-logo image ]
```

Requirements:

- no large combined logo in the sidebar brand row;
- no ordinary `<strong>H!veAI</strong>` duplicate wordmark when the text-logo image is available;
- preserve aspect ratios;
- `object-fit: contain` or equivalent;
- no clipping/cropping of small emblem;
- no stretching of text logo;
- complete brand row fits sidebar width;
- subtitle `Development command center` may remain only if it does not make the header too tall; it may be removed to match reference better.

## C05 — Density / spacing / proportions

Match the reference dashboard's dense professional console treatment:

- smaller/tighter cards;
- restrained vertical gaps;
- compact topbar/header;
- compact KPI strip;
- information-dense project rows;
- thin borders;
- dark navy/black surfaces;
- violet/blue accents with green/yellow/red status colors;
- avoid oversized hero headings;
- central Cockpit is dominant;
- right rail is narrow and vertically stacked;
- bottom activity/metrics remain compact.

Use CSS grid/flex with sensible `minmax()`/`clamp()` behavior rather than brittle one-monitor hard-coded scaling.

## C06 — Internal scroll policy

Allowed bounded internal scrolling when needed:

- Projects rail
- current task/subtasks
- Recent Activity
- AI Assistant message region
- System Status

Not allowed at canonical desktop QA sizes:

- outer Command Center document scroll
- outer horizontal scroll

Do not hide essential content with `overflow:hidden` unless that content remains available in a bounded internal region/control.

## C07 — Preserve all functional/security remediation

The visual recompose must not regress:

- production-mode Tauri no-localhost launcher
- no console window
- SQLite/migrations
- Project Registry
- Git engine
- watcher
- runtime boundary
- CSP hardening
- no legacy sidecar
- default-denied Git mutation
- all M07.03 strict functional/security fixes

## C08 — Visual evidence and manual gate

After all automated work succeeds:

1. republish validated production-mode Tauri executable using corrected rollback-safe publication flow;
2. launch using `Desktop\H!veAI.lnk`;
3. confirm no localhost/5173 error and no external browser shell;
4. compare running Command Center side-by-side with canonical dashboard reference;
5. prove no outer vertical/horizontal scroll at `1536x1024`;
6. prove no outer vertical/horizontal scroll at `2048x1280`;
7. prove Projects rail + central Cockpit + right AI/System rail visible simultaneously;
8. prove Runtime/Database/Watcher are no longer giant stacked rows;
9. prove small logo is fully visible;
10. prove wordmark uses `H!veAI text logo.png`;
11. keep screenshots local/sanitized unless explicitly asked to commit them;
12. final visual state MUST remain:

`PENDING USER VISUAL ACCEPTANCE`

until the user personally reviews the new `.lnk` build.

## C09 — Durable UI governance pointer

`H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md` already exists.

Update `H!veAI/AGENTS.md` with a concise mandatory pointer/summary requiring every future UI-touching milestone to read/preserve it, including:

- single-viewport Command Center;
- canonical reference fidelity;
- small-logo + text-logo rule;
- compact infrastructure status;
- no regression to long multi-screen vertical stacking.

Do not remove the existing Canonical UI Assets section.

---

# PART D — MANUAL ACCEPTANCE ITEMS: NEVER FABRICATE

Unless directly performed with genuine evidence, keep these explicit:

1. user visual acceptance of the refreshed Command Center
2. 1100x760/narrow-window usability if required by prior checklist
3. actual in-app restart-command full cycle

The new visual implementation may be technically complete while final status remains `PENDING USER VISUAL ACCEPTANCE`.

Do not mark the user visual gate PASS yourself.

---

# PART E — SECURITY / ARCHITECTURE CONSTRAINTS

Preserve:

- Rust-native H!veAI runtime
- no always-on Python sidecar
- one SQLite persistence stack
- registered-project boundaries
- no arbitrary shell IPC
- no arbitrary SQL IPC
- no arbitrary watcher path IPC
- Git writes default denied
- no implicit network fetch/push
- no real user project mutation in tests
- no secret logging
- no installer
- no M08 work

Filesystem/Git/DB mutation tests use temporary fixtures.

---

# REQUIRED M07.03 ACCEPTANCE CRITERIA

Automated remediation can be reported complete only if all are true:

1. rescan state machine success/failure semantics are correct;
2. persisted and in-memory rescan states agree;
3. lexical + physical watcher containment implemented;
4. complete M07 focused production-path tests exist;
5. H!veAI diff deterministically strips/prevents binary patch payloads;
6. text/binary/mixed/staged/worktree product-boundary diff matrix passes;
7. migration v6 completes residual timestamp normalization;
8. timestamp format/order/migration/malformed tests pass;
9. non-Git -> Git repair rejects;
10. same Git identity + advanced HEAD works by documented policy;
11. no-remote history continuity fails closed when ambiguous;
12. full repair identity matrix tests pass;
13. SQLite backup is WAL-consistent;
14. backup retention/repeated/failure tests pass;
15. lock/busy behavior bounded;
16. corruption does not overwrite/reinitialize source;
17. full frontend/Rust regression matrix recorded green;
18. no legacy process / zero port 8765 listener;
19. current QA binary comes from proven Tauri production-mode no-bundle build;
20. candidate is smoke-tested with 5173 absent before stable replacement;
21. last-known-good executable is rollback-safe on every publication failure path;
22. `.lnk` targets stable EXE directly and uses canonical small-logo-derived ICO;
23. no console window for normal release launch;
24. Command Center fits one viewport at 1536x1024 with no outer scroll;
25. Command Center fits one viewport at 2048x1280 with no outer scroll;
26. no outer horizontal scroll at those sizes;
27. Projects rail + central Cockpit + right AI/System rail visible simultaneously;
28. giant full-width Runtime/Database/Watcher stacking removed from overview;
29. infrastructure health remains compactly available;
30. sidebar uses canonical `H!veAI small logo.png` emblem;
31. sidebar uses canonical `H!veAI text logo.png` wordmark;
32. neither brand asset clipped/stretched;
33. large combined logo not used in sidebar brand row;
34. `AGENTS.md` contains durable launcher-production/rollback rule;
35. `AGENTS.md` points to/preserves `UI_LAYOUT_GOVERNANCE.md`;
36. TASKS/docs/log truthful;
37. historical logs immutable;
38. no Windows installer introduced;
39. M08 remains unstarted;
40. M07.03 log committed/pushed and final remote HEAD verified.

If any automated BLOCKER or MAJOR remains:

`M07.03 RESULT = FAIL`

If all automated findings close but user visual/restart manual items remain:

`M07.03 RESULT = PENDING HUMAN ACCEPTANCE`

Do not call it unconditional PASS until the independent ChatGPT audit and required human gate authorize it.

---

# Commit / push

Use focused commits.

Required new log:

`H!veAI/docs/H!veAI/codex-logs/M07.03_CONSOLIDATED_STRICT_CLOSURE_CODEX_LOG.md`

Push normally to `origin/H!veAI`.

Do not force push.

After push verify:

- final remote HEAD
- M07.03 log remote-visible
- M07.02A strict audit remote-visible
- this M07.03 prompt remote-visible
- historical logs unchanged
- no M08 prompt/code created

---

# FINAL RESPONSE FORMAT

Return exactly these sections:

1. M07.03 RESULT
2. FETCH / SYNC / BRANCH / FINAL HEAD
3. A01 RESCAN STATE CLOSURE
4. A02 PHYSICAL ROOT CONTAINMENT
5. A03 M07 FOCUSED TEST CLOSURE
6. A04 BINARY DIFF PRODUCT-BOUNDARY CLOSURE
7. A05 TIMESTAMP V6 MIGRATION
8. A06 PROJECT REPAIR IDENTITY MATRIX
9. A07 SQLITE WAL BACKUP / LOCK / CORRUPTION
10. A08 FULL REGRESSION MATRIX
11. A09 TRACKER / DOC TRUTHFULNESS
12. B01 PRODUCTION BUILD PROVENANCE
13. B02 PRE-PUBLICATION CANDIDATE SMOKE
14. B03 LAST-KNOWN-GOOD ROLLBACK SAFETY
15. B04 AGENTS LAUNCHER GOVERNANCE
16. C01 SINGLE-VIEWPORT EVIDENCE
17. C02 CANONICAL DASHBOARD COMPOSITION
18. C03 COMPACT SYSTEM STATUS
19. C04 CANONICAL SMALL LOGO + TEXT LOGO
20. C05 DENSITY / SPACING
21. C06 SCROLL POLICY
22. C07 FUNCTIONAL REGRESSION AFTER UI RECOMPOSE
23. C08 LOCAL VISUAL EVIDENCE / USER ACCEPTANCE STATUS
24. C09 UI GOVERNANCE POINTER
25. CSP / RUNTIME REGRESSION
26. DEPENDENCY / SECURITY RESULTS
27. WINDOWS SMOKE / PORT 5173 / PORT 8765
28. DESKTOP H!veAI.LNK STATUS / TARGET / ICON
29. CONSOLE WINDOW STATUS
30. INSTALLER STATUS — MUST BE NOT IMPLEMENTED
31. FILES ADDED
32. FILES MODIFIED
33. HISTORICAL LOG INTEGRITY
34. M07.03 LOG PATH / GITHUB VERIFICATION
35. COMMITS / PUSH / REMOTE HEAD
36. REMAINING BLOCKERS / MAJORS
37. UNVERIFIED / MANUAL ACCEPTANCE ITEMS
38. EXACT NEXT STEP

If any automated blocker/major remains:

`M07.03 — REMEDIATION CONTINUES`

If automated closure is complete but user visual/restart gates remain:

`M07.03 — PENDING HUMAN ACCEPTANCE`

Do not state M08 is ready. Independent ChatGPT audit must decide that later.