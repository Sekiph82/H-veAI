# M07.07B — Final Visual + Restart Closure

This is the final bounded closure pass for M07 after the independent M07.07A strict audit returned `CONDITIONAL / PENDING HUMAN ACCEPTANCE`.

It is **not M08**.

Do not start M08. Do not create an installer. Do not redesign the current Command Center. Do not redo already-passed Git Diff, Project Registry identity, watcher correctness/evidence, SQLite durability, frontend-ready IPC, launcher rollback, selected-project behavior, or the name-only Command Center Projects rail.

The user has already visually accepted the current Command Center project-selection behavior. This pass exists only to make three final user-requested UI/QA corrections and expose the already-existing native restart command for real manual acceptance.

---

## Exact working locations

Windows Git root:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ`

Windows app root:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI`

GitHub repository:
`https://github.com/Sekiph82/AI-Commerce-HQ`

Branch:
`H!veAI`

Branch URL:
`https://github.com/Sekiph82/AI-Commerce-HQ/tree/H!veAI`

Current independent audit commit entering this pass:
`d1d90f943b002dec7e86fa7f026fa65ce59b11d8`

Mandatory synchronization before reading the prompt:

```powershell
cd "C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ"
git fetch origin H!veAI
git rev-list --left-right --count HEAD...origin/H!veAI
```

If safely behind with no conflicting tracked changes:

```powershell
git merge --ff-only origin/H!veAI
```

Never reset, rebase, force-checkout, or force-push automatically.

---

# Canonical UI Assets — USER OVERRIDE FOR SIDEBAR BRANDING

Canonical visual assets directory:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI`

## New authoritative sidebar brand source

The user explicitly overrides the prior two-image sidebar composition.

Use this exact source image for the visible top-left sidebar brand area:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI\H!veAI logo.png`

The sidebar header must show **only this one combined PNG as one visible brand asset**.

This overrides the older sidebar rule that used:

- `H!veAI small logo.png`
- `H!veAI text logo.png`
- visible `Development command center` text

For the visible sidebar brand area after M07.07B:

- copy the exact `H!veAI logo.png` bytes into a stable repo asset such as `H!veAI/src/assets/hiveai-logo.png`;
- use that copied repo asset at runtime rather than an absolute Windows file path;
- preserve aspect ratio;
- use `object-fit: contain` or equivalent;
- do not crop, redraw, recolor, stretch, reconstruct, split, or add visible text around it;
- do not render a separate emblem beside it;
- do not render a separate H!veAI text-logo beside it;
- do not render the visible `Development command center` subtitle under/beside it;
- accessible `alt="H!veAI"` / screen-reader labeling is allowed and required where appropriate;
- keep the same compact sidebar footprint and do not widen the sidebar simply to accommodate the image.

Before committing, compare SHA-256 or byte identity between the canonical source PNG and the copied repo PNG and record the result in the milestone log.

### IMPORTANT: Desktop shortcut icon rule is unchanged

Do **not** replace the Windows shortcut icon source.

`Desktop\H!veAI.lnk` continues to use the ICO derivative produced from:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI\H!veAI small logo.png`

The new combined `H!veAI logo.png` rule applies only to the visible sidebar brand area.

Update both of these durable governance files so future milestones do not restore the obsolete two-image sidebar composition:

- `H!veAI/AGENTS.md`
- `H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`

The new rule must explicitly state that the sidebar visible branding is the single combined `H!veAI logo.png`, while the Desktop shortcut icon remains derived from `H!veAI small logo.png`.

---

## Read before editing

Read completely after synchronization:

1. `H!veAI/AGENTS.md`
2. `H!veAI/CONSTITUTION.md`
3. `H!veAI/ARCHITECTURE.md`
4. `H!veAI/TASKS.md`
5. `H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`
6. `H!veAI/docs/H!veAI/audits/M07.07A_EVIDENCE_INTEGRITY_CLOSURE_STRICT_AUDIT.md`
7. this prompt

Historical logs and audits are immutable.

---

# Allowed scope

Expected UI/product files:

- `H!veAI/src/components/Shell.tsx`
- `H!veAI/src/pages.tsx`
- `H!veAI/src/styles.css`
- `H!veAI/src/command-center.css` only if the sidebar brand sizing is defined there
- `H!veAI/src/assets/hiveai-logo.png` new exact copied asset

Focused tests:

- `H!veAI/tests/app.test.tsx`
- `H!veAI/tests/m07.06-focused.test.tsx` only when needed for current shell/route regression

Governance / tracker / log:

- `H!veAI/AGENTS.md`
- `H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`
- `H!veAI/TASKS.md`
- new log `H!veAI/docs/H!veAI/codex-logs/M07.07B_FINAL_VISUAL_AND_RESTART_CLOSURE_LOG.md`

Inspect but do not modify unless a direct verification proves something is missing:

- `H!veAI/src-tauri/src/lib.rs`
- `H!veAI/src-tauri/permissions/foundation.toml`
- `H!veAI/src-tauri/capabilities/default.json`

The expected repository state already contains:

- native command `hiveai_request_restart`
- `app.request_restart()`
- permission `allow-request-restart`
- capability registration for `allow-request-restart`

Do not rewrite the native restart implementation if those facts remain true.

Do not change Project Registry/Git/Watcher/SQLite production logic in this pass.

---

# R01 — Replace the visible sidebar brand with the one-piece canonical PNG

Current behavior:

`Shell.tsx` imports and visibly composes:

- `hiveai-small-logo.png`
- `hiveai-text-logo.png`
- `Development command center`

Required target:

The top-left brand area displays only the exact combined image sourced from:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI\H!veAI logo.png`

Implementation requirements:

1. copy that canonical image unchanged into the repo asset path;
2. import only the combined asset for visible sidebar branding;
3. remove the separate visible emblem + text-logo composition from `Shell.tsx`;
4. remove the visible `Development command center` subtitle;
5. render the combined PNG as a single `<img>` or equivalent single image surface;
6. preserve image aspect ratio and full contents;
7. no visible crop/stretch;
8. keep compact alignment within the existing sidebar width;
9. do not delete the historical small/text assets if they remain useful elsewhere;
10. specifically preserve `H!veAI small logo.png` as the authoritative Windows shortcut-icon source.

### Focused branding tests

Add/update tests that prove:

- one visible H!veAI brand image is rendered in the sidebar;
- `Development command center` is no longer visible;
- old separate visible brand composition is not rendered;
- primary navigation remains usable;
- Command Center single-viewport regression remains intact.

Do not make brittle tests that depend on arbitrary pixel width. Source asset identity + visible composition are the contract.

---

# R02 — Expose the real native Restart H!veAI action in Settings

The native restart backend already exists and must remain the single restart mechanism:

```text
Settings button
    ↓
hiveai_request_restart
    ↓
app.request_restart()
```

Do not use:

- `window.location.reload()`;
- spawning a second EXE manually;
- PowerShell/cmd/batch;
- browser reload;
- process-kill tricks;
- a fake success message.

## Settings UI target

Replace the current generic Settings placeholder with a small real Settings page or real Application section containing:

- heading: `Settings`
- section/card: `Application`
- concise explanation that restart relaunches the native H!veAI desktop application
- primary or clearly visible button labeled exactly: `Restart H!veAI`

Tauri desktop behavior:

1. clicking `Restart H!veAI` asks for explicit confirmation, e.g. `Restart H!veAI now?`;
2. cancel means no restart IPC call;
3. confirm calls exactly `invoke('hiveai_request_restart')` or an equivalently bounded typed helper;
4. if IPC returns an error before restart, show a truthful bounded error state;
5. do not claim restart succeeded merely because the button was clicked.

Browser-preview behavior:

- must not invoke native restart;
- button may be disabled or show an honest `Desktop app only` state;
- no attempt to simulate a native restart.

## Existing native boundary verification

Before editing Rust, verify:

- `H!veAI/src-tauri/src/lib.rs` contains `hiveai_request_restart` and calls `app.request_restart()`;
- `H!veAI/src-tauri/permissions/foundation.toml` allows the command;
- `H!veAI/src-tauri/capabilities/default.json` includes `allow-request-restart`.

If all three are present, do not modify them.

## Focused restart tests

At minimum test:

1. Tauri Settings renders `Restart H!veAI`;
2. confirmation cancelled => `hiveai_request_restart` is not invoked;
3. confirmation accepted => `hiveai_request_restart` is invoked exactly once;
4. browser preview cannot invoke native restart;
5. Settings navigation still works.

Automated tests do not close the real restart acceptance gate. The user must manually click the production Settings button after publication and confirm that H!veAI actually exits/relaunches through Tauri.

---

# R03 — Remove stale Projects-page M06 future copy

Current stale visible copy on the Projects page:

`Live Git engine arrives in M06`

M06 is already complete, so this future-tense roadmap copy is false/stale.

Replace it with exactly:

`Live Git metadata and status available`

Keep the Registry Boundary meaning:

- explicit user action required;
- Registry itself does not mutate branch/file/remote state;
- live Git metadata/status is already available.

Do not replace this with another milestone-number sentence.

Search the visible Projects route for equivalent stale `arrives in M06` copy and remove/update it if it is the same historical placeholder. Do not broadly rewrite unrelated product copy.

Add a focused frontend assertion that the old sentence is absent and the new sentence is present on the Projects page.

---

# R04 — Remove the stale milestone suffix from the sidebar version label

Current visible sidebar copy:

`H!veAI 0.1.0 · M07.06`

Required copy:

`H!veAI 0.1.0`

Rules:

- remove `· M07.06`;
- do not replace it with `M07.07`, `M07.07A`, `M07.07B`, `M08`, or any other milestone;
- milestone progress belongs in TASKS/audits/logs, not permanent product chrome;
- keep the `Local foundation online` line unchanged unless a direct regression requires a correction.

Add/update a frontend assertion that `H!veAI 0.1.0` is visible and `M07.06` is not visible in product chrome.

---

# R05 — Preserve all already-approved Command Center behavior

This patch must not regress the user's approved Command Center screenshots.

Preserve:

- one-screen Global Overview at canonical desktop size;
- no outer Command Center scrollbars;
- names-only Projects rail;
- selected-project in-place switching;
- AI-Commerce-HQ ↔ Bulk-Edit switching without navigation;
- explicit `Open cockpit` navigation only;
- no FormuLab fallback flash in live Tauri routes;
- compact Current Project header;
- distinct topbar Command / AI Assistant / Notifications surfaces;
- compact System Status;
- global bottom-center Akilta footer with red heart;
- Project Registry as live project-identity source.

No dashboard redesign.

Do not add live task/workflow/agent claims that are not yet supported by later milestones.

This final M07 closure patch does **not** pull M08 Task Source Discovery into M07.

---

# R06 — Durable governance update

Because the user explicitly changed the canonical sidebar branding rule, update:

- `H!veAI/AGENTS.md`
- `H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`

Required durable truth:

### Sidebar visible brand

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI\H!veAI logo.png`

is the one-piece authoritative visible sidebar brand image.

### Desktop shortcut icon

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI\H!veAI small logo.png`

remains the authoritative source for the Windows shortcut ICO.

Remove/supersede governance statements that require the sidebar to visibly compose `small logo + text logo`.

Do not accidentally change shortcut-icon governance.

---

# R07 — Log and tracker truthfulness

Create before implementation:

`H!veAI/docs/H!veAI/codex-logs/M07.07B_FINAL_VISUAL_AND_RESTART_CLOSURE_LOG.md`

Record:

- synchronized base HEAD;
- exact changed files;
- canonical source PNG path;
- copied repo asset path;
- SHA-256/byte-identity evidence for the copied logo asset;
- restart command source/permission/capability verification;
- exact focused test names and results;
- full regression results;
- production publication result;
- final local/remote HEAD;
- no installer / no M08 statement.

Update `H!veAI/TASKS.md` truthfully:

- M07.07A independent audit = CONDITIONAL / automated closure accepted;
- Command Center selection visual behavior was user-approved from real `Desktop\H!veAI.lnk` screenshots;
- M07.07B is the final visual/restart closure patch;
- M08 remains blocked until the user manually confirms the production restart action and final small visual changes;
- do not mark M07 fully PASS before that manual confirmation.

Historical M07.07/M07.07A logs remain immutable.

---

# Verification

Run focused tests first.

Then full frontend regression from `H!veAI`:

```powershell
npm run typecheck
npm test -- --reporter=dot
npm run build
npm audit --audit-level=high
```

Run Rust regression from Git root because this is the final M07 closure boundary even if Rust is unchanged:

```powershell
cargo fmt --manifest-path H!veAI/src-tauri/Cargo.toml -- --check
cargo check --manifest-path H!veAI/src-tauri/Cargo.toml
cargo test --manifest-path H!veAI/src-tauri/Cargo.toml
cargo build --manifest-path H!veAI/src-tauri/Cargo.toml
```

Run publisher failure harness:

```powershell
powershell -ExecutionPolicy Bypass -File H!veAI\scripts\tests\publish-dev-qa-failure-harness.ps1
```

Only when all automated gates pass, publish the production QA build once:

```powershell
powershell -ExecutionPolicy Bypass -File H!veAI\scripts\publish-dev-qa.ps1
```

Confirm:

- `Desktop\H!veAI.lnk` still targets the stable `dev-bin\H!veAI.exe`;
- shortcut icon remains the ICO derived from `H!veAI small logo.png`;
- no browser or visible terminal is used for launch;
- no installer was created.

---

# Final manual acceptance contract

After publication, the user will manually verify the real stable Desktop shortcut build.

Required final M07 human checks:

1. Launch `Desktop\H!veAI.lnk`.
2. Confirm top-left sidebar shows only the one-piece canonical `H!veAI logo.png` image, with no separate emblem/text-logo and no visible `Development command center` subtitle.
3. Open Projects and confirm `Live Git engine arrives in M06` is gone and `Live Git metadata and status available` is shown.
4. Confirm sidebar shows `H!veAI 0.1.0` with no milestone suffix.
5. Open Settings.
6. Click `Restart H!veAI`.
7. Confirm the restart prompt.
8. Verify the native H!veAI desktop application actually exits/relaunches automatically through Tauri.
9. Verify no terminal window and no browser window appears during restart.
10. Verify H!veAI returns usable after restart.

Builder/Codex cannot self-approve steps 1–10.

Final builder status must be:

`M07.07B RESULT = PENDING USER FINAL M07 ACCEPTANCE`

when automated gates pass.

Only after the user confirms the final visual checks and real in-app restart may the independent auditor close M07 and authorize M08.

---

# Acceptance criteria

AC01 — Sidebar uses one visible combined canonical H!veAI PNG only.

AC02 — Copied repo logo asset is byte-identical/SHA-256-identical to canonical `H!veAI logo.png`.

AC03 — Visible `Development command center` subtitle is removed.

AC04 — Old separate small-logo + text-logo visible sidebar composition is removed.

AC05 — Desktop shortcut icon source remains `H!veAI small logo.png` / its stable ICO derivative.

AC06 — Settings exposes `Restart H!veAI` in Tauri desktop mode.

AC07 — Cancelled restart confirmation sends no restart IPC.

AC08 — Confirmed restart sends exactly `hiveai_request_restart`.

AC09 — Browser preview does not simulate/invoke a native restart.

AC10 — Existing native restart command, permission, and capability are verified and preserved.

AC11 — Projects page displays `Live Git metadata and status available` and not `Live Git engine arrives in M06`.

AC12 — Sidebar version displays `H!veAI 0.1.0` with no milestone suffix.

AC13 — AGENTS/UI governance are updated to the new single combined sidebar-logo rule without changing the shortcut-icon rule.

AC14 — User-approved Command Center layout/selection behavior is preserved.

AC15 — Full frontend/Rust regression, publisher harness, production build, and stable publication pass.

AC16 — New M07.07B log and TASKS are truthful.

AC17 — No M08 code/prompt implementation is started by the builder and no Windows installer is created.

AC18 — Real in-app restart remains a manual acceptance item until the user actually performs it.

Stop after M07.07B implementation, log, commit, push, and stable QA publication.
