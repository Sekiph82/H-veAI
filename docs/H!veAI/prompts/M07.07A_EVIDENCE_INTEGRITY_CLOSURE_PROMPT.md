# M07.07A — Evidence Integrity Closure

This is a bounded remediation after the independent M07.07 strict audit returned **FAIL**.

It is **not M08**.

Do not start M08. Do not create an installer. Do not redesign the current dashboard. Do not redo already-passed Git Diff, frontend-ready IPC, Registry production identity policy, or selected-project UI behavior unless a new direct test exposes a real regression.

The goal is to replace misleading/name-only tests with real failure injection / transition evidence and to correct tracker truthfulness in a new log.

---

## Exact working locations

Windows Git root:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ`

Windows app root:
`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI`

GitHub repository:
`https://github.com/Sekiph82/AI-Commerce-HQ`

Branch:
`H!veAI`

Branch URL:
`https://github.com/Sekiph82/AI-Commerce-HQ/tree/H!veAI`

Mandatory fetch-before-prompt:

```powershell
cd "C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ"
git fetch origin H!veAI
git rev-list --left-right --count HEAD...origin/H!veAI
```

If safely behind with no conflicting tracked changes:

```powershell
git merge --ff-only origin/H!veAI
```

Never reset/rebase/force-checkout/force-push automatically.

---

# Canonical UI Assets

Canonical UI reference assets:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI`

Authoritative assets include:

- dashboard reference image
- Akilta logo
- H!veAI logo
- `H!veAI small logo.png`
- `H!veAI text logo.png`

Preserve exactly:

```text
[ H!veAI small logo.png ] [ H!veAI text logo.png ]
```

Do not crop, recolor, stretch, redraw, or replace these assets.

Preserve the existing single-viewport Global Overview, names-only Projects rail, in-place project selection, explicit `Open cockpit` navigation, distinct topbar surfaces, compact System Status, and global Akilta footer.

Also obey:

`H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`

No UI redesign is in scope.

---

## Read before editing

Read completely:

1. `H!veAI/AGENTS.md`
2. `H!veAI/docs/H!veAI/audits/M07.07_CLAUDE_SURGICAL_REMEDIATION_STRICT_AUDIT.md`
3. `H!veAI/docs/H!veAI/prompts/M07.07_CLAUDE_SURGICAL_REMEDIATION_PROMPT.md`
4. `H!veAI/docs/H!veAI/codex-logs/M07.07_CLAUDE_SURGICAL_REMEDIATION_LOG.md` as historical claims only
5. this prompt

Historical M00-M07.07 builder logs are immutable. Do not rewrite the M07.07 Claude log even though the audit found inaccurate claims. Correct the record in the new M07.07A log and TASKS.md.

---

## Allowed files

Modify only these unless a directly imported private test helper is strictly required:

SQLite:
`H!veAI/src-tauri/src/db/mod.rs`

Watcher:
`H!veAI/src-tauri/src/watcher.rs`

Frontend tests:
`H!veAI/tests/m07.06-focused.test.tsx`

Publisher harness:
`H!veAI/scripts/tests/publish-dev-qa-failure-harness.ps1`

Production publisher only if a real test exposes a bug:
`H!veAI/scripts/publish-dev-qa.ps1`

Tracker:
`H!veAI/TASKS.md`

New log:
`H!veAI/docs/H!veAI/codex-logs/M07.07A_EVIDENCE_INTEGRITY_CLOSURE_LOG.md`

Do not edit Registry production/tests unless an unrelated regression is discovered. The 15-case Registry matrix passed independent source audit.

---

# A01 — SQLite: real busy/locked contention

Current misleading test:

`sqlite_busy_locked_contention_respects_configured_bounds`

It currently reads `PRAGMA busy_timeout` only. That is not contention evidence.

Replace/strengthen it so it uses the real DB path and at least two connections:

1. initialize a temp H!veAI DB;
2. connection A acquires a write lock (`BEGIN IMMEDIATE` / equivalent deterministic SQLite write lock);
3. connection B is obtained through production `DatabaseState::open_connection()`;
4. connection B attempts a conflicting write;
5. assert the operation fails with BUSY/LOCKED rather than hanging indefinitely;
6. measure elapsed time and prove it remains inside a bounded window consistent with the 5000ms configured busy timeout, with a reasonable CI tolerance;
7. release connection A cleanly.

Do not replace this with merely reading the pragma.

Record exact elapsed bound in the log.

---

# A02 — SQLite: real backup-publication failure injection

Current misleading test:

`sqlite_backup_publication_failure_preserves_source_and_prior_backup`

It currently performs two successful backups and never induces failure.

Add a **private test-only failpoint**, `#[cfg(test)]` only, at the backup publication/rotation boundary. The public/production API must expose no bypass.

Preferred design:

- keep normal `create_migration_backup()` unchanged for production callers;
- factor a private internal function with an optional `#[cfg(test)]` failure stage or a test-only helper wrapper;
- production always uses `NoFailure` / no hook;
- test can deterministically fail after the temporary logical backup has been produced but before final publication, or during final publication.

Direct test must:

1. create source DB with known data;
2. create a known prior good `.bak`;
3. record source and prior-backup hashes/content;
4. change source so a new backup would differ;
5. trigger the deterministic publication failpoint;
6. assert function returns error;
7. assert source DB remains logically intact;
8. assert prior good `.bak` is restored/preserved byte-for-byte or logically identical as appropriate to the publication algorithm;
9. assert no broken partial final backup replaces it;
10. assert temporary artifact cleanup is bounded/truthful.

Do not label a success-path rotation test as publication-failure evidence.

---

# A03 — SQLite: backup failure must block migration mutation

Current misleading test:

`sqlite_backup_failure_prevents_migration_mutation`

It does not trigger backup failure.

Use the same private test-only failure mechanism through a production-equivalent initialization path.

Required fixture:

1. create a valid database at schema v6;
2. insert a `project_snapshots.created_at` numeric value that migration v7 would convert;
3. establish any required prior good backup;
4. invoke initialization with the private backup-publication failure injected;
5. assert initialization fails before migration v7 is committed;
6. reopen safely and prove migration max version remains 6;
7. prove the numeric `project_snapshots.created_at` value remains unconverted;
8. prove no v7 migration row was inserted.

Private `cfg(test)` initialization/hook plumbing is allowed. No public runtime flag, CLI flag, IPC, environment variable, or production bypass.

---

# A04 — Watcher: real failure/event/lifecycle evidence

Do not add more tests whose names describe failures that never happen.

Strengthen or replace the M07.07 watcher tests as follows.

## A04.1 attachment failure

`watcher_attachment_failure_preserves_rescan_requirement`

Must actually traverse `WatcherManager::configure_project()` attachment-failure control flow.

Because OS watcher failure is hard to force portably, a private `#[cfg(test)]` watcher-backend/attach failpoint is acceptable. It must be impossible to activate from production IPC/config.

Test:

- seed prior `rescan_required=true`;
- invoke configure/refresh path with attach failure injected;
- assert returned error/degraded state as appropriate;
- assert `rescan_required` remains true;
- assert no watcher handle/root is installed.

## A04.2 Git refresh failure

`watcher_git_refresh_failure_preserves_rescan_requirement`

Must use a registered **Git repository**, then make the real Git snapshot operation fail deterministically, e.g. invalidate required Git metadata after registration or use a private test-only failpoint around the internal snapshot call if necessary.

Test must call `refresh_project_snapshot()` with a GitMetadata event, observe an error, and prove prior `rescan_required=true` remains true in memory and no false successful reconciliation is persisted.

## A04.3 project-snapshot persistence failure

`watcher_snapshot_persistence_failure_preserves_rescan_requirement`

Must make the real project-snapshot INSERT fail, e.g. controlled temp-fixture schema sabotage or private test-only DB failpoint.

Test must observe error and prove prior `rescan_required=true` is not cleared in memory.

## A04.4 unchanged refresh duplicate prevention

Assert actual internal watcher state, not merely number of project status records:

- capture `inner.watches.len()` and `inner.watch_roots` before;
- refresh same registry/root;
- assert exactly one watcher for project and same root afterward.

## A04.5 repaired root reattachment

After Registry path repair and `refresh_from_registry()`:

- assert `inner.watch_roots[project_id] == canonical/new expected root`;
- assert old root is no longer the active root for that project;
- assert one watcher handle remains.

## A04.6 event on repaired root

Do not merely assert `status.available`.

After reattach:

- capture previous event/snapshot evidence;
- write a file under new root;
- wait with a bounded polling loop, not an unconditional long sleep;
- prove `last_event_at`, changed-path count, or a new persisted `project_snapshots` row changes due to the event;
- prove an event under the old root is not incorrectly attributed after reattach if practical.

## A04.7 Git-category event persists Git snapshot

Use an actual initialized/committed Git temp repo.

After a GitMetadata event:

- call production refresh path;
- assert `git_snapshots` row count increases;
- assert newest `project_snapshots.git_snapshot_id` is non-null and references the persisted Git snapshot.

Do not use `project_snapshots` count alone as proof.

## A04.8 non-Git-category event skips Git snapshot

Use a **Git registered project** but emit a Source/Config event.

Assert project snapshot may persist while `git_snapshots` count does not increase.

This proves category gating, not merely non-Git-folder behavior.

## A04.9 manager Drop

Retain an `Arc`/test-visible reference to inner state before dropping manager.

After `drop(manager)` returns:

- assert `running == false`;
- assert watcher-handle map empty;
- assert watch-root map empty;
- the fact that Drop returns after `join()` plus state assertions is acceptable worker-release evidence.

## A04.10 physical symlink/junction containment

Attempt to create a real link inside root that resolves outside.

- Unix: use `std::os::unix::fs::symlink`.
- Windows: attempt `std::os::windows::fs::symlink_file` / symlink_dir or another safe temp-only link mechanism.

If the Windows environment denies link creation due privileges, do **not** silently PASS the security case. Record in the new log:

`UNVERIFIED — link creation denied by environment: <exact OS error>`

The unit test may skip only after recording/printing the exact capability error. An ordinary outside file is not a substitute for a symlink escape.

---

# A05 — Frontend: real Registry transitions, not pre-baked final state

Keep existing good M07.06 selection tests.

Strengthen the four weak M07.07 cases.

## A05.1 newly registered refresh

The project must **not** exist in the initial `hiveai_projects_list` result.

Then simulate a real Registry transition through the UI or shared Registry refresh path:

1. initial rail does not contain new project;
2. registration/refresh changes mocked backend list;
3. trigger the actual app/provider refresh mechanism;
4. assert new project appears in Command Center rail without remount/restart.

## A05.2 archive/remove refresh

Start with the project visible.

Then:

1. perform the real UI/provider archive/remove operation or equivalent real shared Registry refresh path;
2. mutate mocked backend result only when operation occurs;
3. refresh;
4. prove project disappears from both Command Center rail and sidebar shortcuts in the same mounted application session.

Do not start the test with the record already absent.

## A05.3 same-instance stale route race

Do not unmount the whole application between routes.

Within one mounted `App` / Router instance:

1. start delayed `/projects/ai-commerce-hq` lookup;
2. navigate to `/projects/bulk-edit` before first promise resolves;
3. resolve Bulk-Edit normally;
4. then resolve the stale AI-Commerce-HQ promise;
5. assert Bulk-Edit remains the visible project and selected project state is not reverted.

## A05.4 browser-preview isolation

In non-Tauri preview:

- positively assert an expected explicit preview fixture identity/surface exists;
- assert live Tauri Registry identity from the mocked live set is not masquerading as the authoritative source;
- assert no Tauri project-get/list call is required for the preview identity behavior if mock instrumentation allows it.

The goal is explicit isolation, not merely absence of a loading message.

---

# A06 — Publisher: actual spawned-process cleanup on failed smoke

Current case 7 merely checks `$staged` and never launches a process.

Keep the harness strictly temp-only and unable to address the real Desktop shortcut or real `dev-bin`.

Add an isolated test-smoke helper that:

1. spawns a harmless bounded child process, preferably PowerShell itself with a long sleep, and captures PID;
2. simulates readiness/smoke failure after spawn;
3. runs cleanup in `finally` using the same bounded cleanup principle as production;
4. asserts process has exited / PID is no longer alive;
5. asserts temp candidate/staged artifacts are cleaned as required;
6. reports `PASS failed_smoke_path_no_spawned_test_process` only after these assertions.

Never launch or kill the real H!veAI stable executable from the failure harness.

---

# A07 — New log and truthful tracker

Create before implementation:

`H!veAI/docs/H!veAI/codex-logs/M07.07A_EVIDENCE_INTEGRITY_CLOSURE_LOG.md`

For every test include:

- exact test name;
- exact production function/control-flow exercised;
- exact failure injected or state transition performed;
- whether it is `REAL_PRODUCTION_PATH_WITH_TEST_FAILPOINT`, `REAL_OS/DB_FAILURE`, `REAL_UI_TRANSITION`, or `UNVERIFIED`;
- command/result;
- any environment limitation.

Do not edit historical M07.07 Claude log.

Update `TASKS.md` truthfully:

- M07.07 strict audit = FAIL because evidence integrity remained incomplete;
- M07.07A = ACTIVE until independent audit;
- do not state human-only pending until this pass independently audits clean;
- M08 remains unstarted/blocked.

---

# Verification

Run focused tests first.

Then full regression:

Frontend from `H!veAI`:

```powershell
npm run typecheck
npm test -- --reporter=dot
npm run build
npm audit --audit-level=high
```

Rust from Git root:

```powershell
cargo fmt --manifest-path H!veAI/src-tauri/Cargo.toml -- --check
cargo check --manifest-path H!veAI/src-tauri/Cargo.toml
cargo test --manifest-path H!veAI/src-tauri/Cargo.toml
cargo build --manifest-path H!veAI/src-tauri/Cargo.toml
```

Run:

```powershell
powershell -ExecutionPolicy Bypass -File H!veAI\scripts\tests\publish-dev-qa-failure-harness.ps1
```

Only when all automated gates pass, run production publication once:

```powershell
powershell -ExecutionPolicy Bypass -File H!veAI\scripts\publish-dev-qa.ps1
```

Do not create an installer.

---

# Final acceptance rule

Final status may be:

- `M07.07A RESULT = FAIL` if any automated MAJOR remains;
- `M07.07A RESULT = PENDING USER VISUAL + M01 RESTART ACCEPTANCE` only if the direct evidence above genuinely closes.

Do not start M08.

Stop after M07.07A log + commit + push.
