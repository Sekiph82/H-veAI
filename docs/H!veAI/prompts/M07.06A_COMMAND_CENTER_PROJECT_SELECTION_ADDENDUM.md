# M07.06A — Command Center In-Place Project Selection Addendum

This addendum is authoritative for the active M07.06 remediation. It does not start M08 and does not expand task-source discovery scope.

If any wording in `M07.06_FOCUSED_EVIDENCE_AND_RUNTIME_TRUTH_CLOSURE_PROMPT.md` conflicts with this addendum about Command Center Projects-rail click behavior, this addendum wins.

Do NOT create a Windows installer.
Do NOT redesign the canonical dashboard composition.
Do NOT reintroduce outer Command Center scrolling or visible nested scrollbars in the core cockpit blocks.

---

# Canonical UI Assets

Canonical UI reference assets are located at:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI`

Use the assets in this folder as authoritative visual references:

- dashboard reference image
- Akilta logo
- H!veAI logo
- `H!veAI small logo.png`
- `H!veAI text logo.png`

Do not redesign, recolor, crop, stretch, replace, or reinterpret these assets.

Preserve the canonical sidebar composition:

```text
[ H!veAI small logo.png ] [ H!veAI text logo.png ]
```

Also obey:

`H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`

---

## User manual-QA requirement

The user explicitly requires the Command Center / Global Overview Projects rail to behave like a project selector for the central Current Project panel.

The current implementation navigates away on row click:

```tsx
onClick={() => navigate(`/projects/${project.id}`)}
```

That is not the requested Global Overview interaction.

## Required behavior

On the Command Center route only:

1. clicking a row in the left `Projects` rail must remain on the Command Center / Global Overview route;
2. the clicked project becomes `selectedProjectId` in the shared session-level registry/project state;
3. the selected row gets a clear but restrained active style;
4. the central `CURRENT PROJECT` header immediately changes to the selected registered project's exact identity;
5. all registry-backed central fields must derive from the same selected record;
6. task/workflow/metrics fields that do not yet have M08/M09 evidence must render neutral truthful placeholders and must not retain another project's data;
7. selecting `Bulk-Edit` must update the central workspace to `Bulk-Edit` without page navigation, reload, or restart;
8. selecting `AI-Commerce-HQ` must update it back to `AI-Commerce-HQ` the same way;
9. selected project remains selected when the user returns to Command Center during the same app session;
10. if selected project is removed/archived or no longer eligible, choose a deterministic live Registry fallback and synchronize `selectedProjectId`;
11. no stale async request may overwrite a newer selection.

Do not use fixture identity in Tauri mode.

## Command Center Projects rail visual simplification

The user supplied new annotated manual-QA evidence and explicitly wants the Projects selector to be much cleaner and denser.

For the **Global Overview Projects rail only**, each row must display **only the project name**.

Do not render any of these inside the row:

- project logo / initials badge / square icon;
- `ACTIVE`, `MISSING`, `ARCHIVED`, or any other status text;
- `Healthy`, `Watch`, `Blocked`, or other health text;
- progress percentage;
- progress bar;
- phase/milestone subtitle;
- `Registered...` subtitle;
- actor or other metadata.

Target visual model:

```text
Projects

AI-Commerce-HQ
Bulk-Edit
fmcg-erp-system
FormuLab
Kenya FMCG
LLM
...
```

Rules:

1. use the saved real project `name` as the visible row label;
2. keep rows compact enough to show materially more project names in the fixed-height one-screen rail;
3. selected project may use a restrained background/border/accent indicator, but do not add status/health badges to communicate selection;
4. active state must remain accessible through `aria-selected`, `aria-pressed`, or another correct semantic signal;
5. for very long names, use a clean single-line truncation only when necessary and expose the full name accessibly (for example native title/tooltip or accessible label);
6. retain the `View all projects` action at the bottom;
7. the rail may retain bounded internal scrolling because it is a genuinely variable-length collection, but the scrollbar should be visually restrained and must not make rows wider or clutter the selector;
8. this simplification applies only to the Command Center project selector. The full Projects page may continue showing Git/status/priority metadata.

The purpose is to make this surface a fast project picker, not a miniature project-status table.

## Separate full-cockpit navigation

The central selected-project workspace must expose a distinct `Open cockpit` action.

That action, and only that explicit cockpit action from the Global Overview project-selection interaction, should navigate to:

```text
/projects/<selectedProjectId>
```

The route must open the exact selected project with the existing no-cross-project-flicker contract.

Sidebar Project Shortcuts and the full Projects page may keep their route-oriented behavior unless the active M07.06 contract says otherwise. The specific new requirement here is that the **Global Overview Projects rail selects in place**.

## Accessibility

- selected row exposes an accessible selected/current state, e.g. `aria-pressed`, `aria-selected`, or equivalent semantically correct implementation;
- keyboard activation changes selected project without navigating away;
- active styling is not conveyed by color alone.

## Required frontend focused tests

Add direct tests proving all of these:

1. `command_center_project_rail_click_selects_in_place_without_navigation`
   - start at `/`;
   - click `Bulk-Edit` registry row;
   - URL/path remains `/`;
   - central Current Project changes to `Bulk-Edit`.

2. `command_center_selection_can_switch_between_live_projects`
   - select Bulk-Edit;
   - select AI-Commerce-HQ;
   - central identity follows each selection exactly.

3. `command_center_selected_row_has_accessible_active_state`
   - selected row is programmatically distinguishable.

4. `command_center_open_cockpit_uses_current_selected_project_id`
   - select Bulk-Edit;
   - click `Open cockpit`;
   - route becomes `/projects/<Bulk-Edit-id>` and never another project.

5. `command_center_selection_persists_when_returning_during_session`
   - select a project;
   - navigate to its cockpit;
   - return to Command Center;
   - selected/current project remains the same.

6. `removed_selected_project_uses_deterministic_live_registry_fallback`
   - remove/archive selected mocked record and refresh;
   - no stale identity remains;
   - another valid registered project is selected deterministically or a truthful no-project state is shown.

7. `stale_async_result_cannot_overwrite_newer_command_center_selection`
   - delay one registry/project result;
   - choose another project;
   - late result must not revert the central identity.

8. `command_center_project_rail_rows_render_name_only`
   - use Tauri-mode live Registry fixtures;
   - rail rows contain the exact project names;
   - rows do not render project initials/logo marks, status strings, health strings, percentages, progress bars, phase labels, or registration subtitles.

9. `command_center_project_rail_long_name_preserves_accessible_full_name`
   - a long project name may visually truncate;
   - full project identity remains available to accessibility/title semantics.

These tests must exercise the real shared RegistryProvider/CommandCenter behavior, not a helper-only model.

---

## Manual QA after stable publication

After the full M07.06 automated gate passes and `Desktop\H!veAI.lnk` is republished:

1. open Global Overview;
2. confirm each Projects rail row contains only a project name, with no logo/initial badge, ACTIVE/health labels, percentage, progress bar, phase, or Registered subtitle;
3. confirm the denser rail visibly fits more project names than the previous version;
4. click `Bulk-Edit` in the Projects rail;
5. confirm the page remains Global Overview;
6. confirm `CURRENT PROJECT` changes to Bulk-Edit immediately;
7. click AI-Commerce-HQ;
8. confirm central Current Project changes back immediately;
9. verify selected-row styling follows the selection without adding status/health clutter;
10. click `Open cockpit` and confirm the exact selected project's cockpit opens;
11. return to Global Overview and confirm the selection remains during the session;
12. confirm no unrelated FormuLab/fixture identity appears at any point.

Final visual status remains `PENDING USER VISUAL ACCEPTANCE` until the user personally confirms the refreshed stable `.lnk` build.

Do not start M08.
