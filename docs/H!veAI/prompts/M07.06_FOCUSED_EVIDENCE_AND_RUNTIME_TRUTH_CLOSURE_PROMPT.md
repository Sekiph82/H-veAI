# M07.06 — Focused Evidence + Runtime Truth Closure

You are continuing H!veAI after the independent M07.05 strict audit returned **FAIL**.

This is a bounded remediation/evidence pass. It is **not M08**.

Do NOT start M08.
Do NOT create an M08 prompt.
Do NOT create a Windows installer.
Do NOT redesign the current single-viewport Command Center.

The goal is to close only the independently verified remaining M07.05 automated findings, prove them with direct production-boundary tests, republish the stable QA build, and stop for independent audit + user visual acceptance.

---

## Mandatory fetch-before-prompt sync

From:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ`

run:

```powershell
git fetch origin H!veAI
git rev-list --left-right --count HEAD...origin/H!veAI
```

If safely behind with no conflicting tracked changes:

```powershell
git merge --ff-only origin/H!veAI
```

Never reset/rebase/force-checkout/force-push automatically.

Canonical branch: `H!veAI`
Canonical app root: `C:\Users\sekip\Desktop\AI-Commerce-HQ files\AI-Commerce-HQ\H!veAI`

Preserve unrelated stash/untracked user files.

---

# Canonical UI Assets

Canonical UI reference assets are located at:

`C:\Users\sekip\Desktop\AI-Commerce-HQ files\H!veAI`

Authoritative assets include:

- dashboard reference image
- Akilta logo
- H!veAI logo
- `H!veAI small logo.png`
- `H!veAI text logo.png`

Do not redesign/recolor/crop/stretch/replace these assets.

Preserve:

```text
[ H!veAI small logo.png ] [ H!veAI text logo.png ]
```

Preserve `H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md` and the current single-viewport composition.

No regression to multi-screen stacked dashboard layout.

---

## Read completely before coding

Read:

- `H!veAI/AGENTS.md`
- `H!veAI/CONSTITUTION.md`
- `H!veAI/ARCHITECTURE.md`
- `H!veAI/TASKS.md`
- `H!veAI/docs/H!veAI/UI_LAYOUT_GOVERNANCE.md`
- `H!veAI/docs/H!veAI/audits/M07.05_BOUNDED_CORRECTNESS_STRICT_AUDIT.md`
- `H!veAI/docs/H!veAI/prompts/M07.05_BOUNDED_CORRECTNESS_AND_EVIDENCE_REMEDIATION_PROMPT.md`
- `H!veAI/docs/H!veAI/codex-logs/M07.05_BOUNDED_CORRECTNESS_AND_EVIDENCE_REMEDIATION_CODEX_LOG.md`
- this prompt

Treat prior aggregate `50 Rust / 11 frontend` results as regression evidence only, not focused closure.

---

## Durable M07.06 log

Create before implementation:

`H!veAI/docs/H!veAI/codex-logs/M07.06_FOCUSED_EVIDENCE_AND_RUNTIME_TRUTH_CLOSURE_CODEX_LOG.md`

Historical logs are immutable.

For every section below record:

- source symbol/file;
- exact previous defect/gap;
- exact correction;
- exact direct test names;
- whether each test exercises production function or helper;
- command/result;
- any intentionally UNVERIFIED manual item.

---

# C01 — MAJOR — Deterministic Git diff product boundary

File:

`H!veAI/src-tauri/src/git_engine/mod.rs`

Current remaining defects:

1. product text-diff args include `--no-ext-diff` but not required `--no-textconv`;
2. legacy `Binary files ...` parsing can add `/dev/null` for a newly-added staged binary;
3. `--numstat` already supplies deterministic actual binary paths, so legacy metadata must not pollute the result;
4. direct real `diff(database, GitDiffRequest)` matrix is still missing.

## Required production behavior

- text diff for both scopes uses fixed args including `--no-ext-diff` and `--no-textconv`;
- no product-path `--binary`;
- matching-scope `--numstat` is authoritative binary metadata;
- binary paths are normalized repository-relative actual paths;
- `/dev/null` is never returned in `binary_files`;
- fallback sanitizer remains only for unexpected binary-patch input;
- sanitize before truncation;
- mixed text+binary retains textual hunks and marks only actual binary files.

Remove the old human-readable binary parser if no longer necessary, or strictly filter it so it can never introduce `/dev/null`/wrong path. Prefer one authoritative metadata source.

## Required tests, all real product boundary unless explicitly parser-level

Build temp repos, register them through H!veAI Registry/DB, call real `diff()`:

1. `diff_text_working_tree_product_boundary`
2. `diff_text_staged_product_boundary`
3. `diff_binary_working_tree_metadata_only`
4. `diff_new_binary_staged_returns_real_path_not_dev_null`
5. `diff_mixed_working_tree_keeps_text_and_binary_metadata`
6. `diff_mixed_staged_keeps_text_and_binary_metadata`
7. `diff_return_never_contains_nul_or_binary_patch_payload`
8. `diff_truncation_is_after_sanitization`

Parser-only helpers may separately test unexpected `GIT binary patch` fallback and mixed-file classification, but do not label helper-only tests as full product-boundary coverage.

---

# C02 — MAJOR — Restore dedicated frontend-ready signal

Files:

- `H!veAI/src-tauri/src/lib.rs`
- `H!veAI/src/main.tsx`
- `H!veAI/src-tauri/permissions/foundation.toml`
- `H!veAI/src-tauri/capabilities/default.json`
- `H!veAI/scripts/publish-dev-qa.ps1`

Current defect:

- dedicated `hiveai_frontend_ready` was removed;
- `main.tsx` no longer invokes it;
- `hiveai_database_status` still emits `HIVEAI_FRONTEND_READY`;
- no dedicated readiness ACL exists.

## Required behavior

Restore a narrow command such as:

```rust
#[tauri::command]
fn hiveai_frontend_ready() {
    log::info!("HIVEAI_FRONTEND_READY");
}
```

Requirements:

- marker emitted by exactly one Rust production site: the dedicated command;
- native/database/runtime commands must not emit it;
- React invokes it after root/application mount when Tauri IPC is usable;
- explicit narrow permission/capability entry allows only this command;
- publisher baseline logic continues to wait for a NEW marker from candidate/stable launch;
- no arbitrary log/file/shell IPC.

Add tests/source assertions proving:

1. dedicated command is in Tauri handler;
2. permission/capability includes it;
3. marker string has one intended Rust emission site;
4. frontend startup invokes only dedicated readiness command for readiness.

---

# C03 — MAJOR — Hash-proven launcher rollback + isolated failure harness

File:

`H!veAI/scripts/publish-dev-qa.ps1`

Preserve current good properties:

- no `-SkipBuild`;
- no external Candidate parameter;
- self-run `npm run tauri:build -- --no-bundle`;
- candidate smoke before swap;
- stable-path smoke after swap;
- direct `.lnk` target;
- canonical icon;
- no installer.

Add:

- SHA-256 of old stable before swap when it exists;
- on any post-swap failure restore rollback;
- verify restored stable SHA-256 equals pre-swap hash exactly;
- hard error if hash restoration cannot be proven;
- bounded cleanup remains mandatory.

Create a **separate temp-path-only test harness** under `H!veAI/scripts/tests/` or equivalent. It must be structurally unable to target the real Desktop shortcut/stable EXE.

Required failure/success cases:

1. invalid PE -> stable unchanged;
2. provenance/build failure -> stable unchanged;
3. readiness failure -> stable unchanged;
4. shortcut/icon precondition failure -> stable unchanged;
5. simulated post-swap failure -> prior stable restored + exact hash;
6. locked stable -> bounded clear failure + bytes unchanged;
7. failed smoke -> no candidate process remains;
8. successful temp swap -> final hash equals candidate;
9. production publisher interface still exposes no bypass flags;
10. final real stable publication succeeds after all project tests.

Do not weaken the production publisher for testability.

---

# C04 — EVIDENCE CLOSURE — Watcher R01/R02

Production code is directionally improved. Do not rewrite it unless a direct test finds a real defect.

Add the missing direct focused tests from M07.05, including at minimum:

### Rescan

- overflow/error marks rescan true;
- ordinary refresh with non-empty events preserves true;
- ordinary refresh with empty events preserves true;
- healthy explicit rescan clears true;
- newest DB row == in-memory final state;
- missing-root explicit attempt preserves required state;
- watcher attachment failure preserves requirement;
- Git refresh failure preserves requirement;
- persistence failure preserves requirement;
- healthy explicit rescan with no prior requirement deterministic.

### Watch-root lifecycle

- positive in-root rename old/new paths;
- rename endpoint outside root rejected;
- duplicate watcher prevention on unchanged refresh;
- repair old root -> new root -> refresh reattaches to new root;
- event on repaired root is observed;
- Git-category event persists bounded Git snapshot;
- non-Git event does not attempt Git snapshot persistence;
- initialization tolerates missing project with degraded state;
- manager drop joins/releases worker/watchers;
- no arbitrary watch-path/file-read IPC;
- lexical and physical containment cases.

Tests must use production functions/manager behavior, temp DBs/dirs only.

---

# C05 — EVIDENCE CLOSURE — Registry R03

Production code now rejects legacy rowless non-Git -> Git. Prove the full contract with direct `repair_project_path()` tests:

1. legacy rowless non-Git -> moved non-Git accepted;
2. legacy rowless non-Git -> Git rejected;
3. explicit non-Git -> Git rejected;
4. same remote/same HEAD accepted;
5. same remote/advanced HEAD accepted;
6. different remote rejected;
7. remote disappearance fails closed under current policy;
8. no-remote exact HEAD accepted;
9. no-remote ancestor accepted;
10. unrelated no-remote rejected;
11. missing old HEAD rejected;
12. credential-bearing equivalent HTTPS remote sanitation comparison;
13. duplicate normalized path rejected;
14. unknown project ID rejected;
15. candidate repo evidence unchanged by validation.

Do not add network fetch/pull/checkout/reset/config mutation.

---

# C06 — EVIDENCE CLOSURE — Migration + SQLite R04/R05

Do not rename/rewrite published migration identities.

Prove migration v7 and historical immutability with real `apply_migrations()` fixtures:

- fresh v7/seven rows;
- apply 1..6, insert numeric `project_snapshots.created_at`, then apply v7 and verify conversion;
- valid RFC3339 remains byte-identical;
- malformed string preserved;
- rerun is ALREADY_CURRENT;
- history mismatch safe failure;
- intentional migration failure rollback;
- timestamp generator RFC3339/millisecond/lexical-order tests.

Add SQLite durability tests with temp DBs only:

- committed WAL data in logical backup;
- second backup and bounded retention;
- backup publication failure preserves source and prior good backup;
- busy/locked contention respects configured bound;
- corrupted DB initialization fails without changing corrupt source bytes;
- healthy quick_check;
- every `DatabaseState::open_connection()` has FK/WAL/busy/synchronous policy;
- backup failure prevents migration schema mutation.

Private/internal test injection is allowed; no public bypass.

---

# C07 — MAJOR — Live Current Project session state + UI truthfulness

Files:

- `H!veAI/src/registryContext.tsx`
- `H!veAI/src/pages.tsx`
- `H!veAI/src/components/Shell.tsx` only if needed

Current remaining defects:

1. Command Center uses `const current = liveProjects[0]`; user selection is not preserved;
2. in Tauri mode, task evidence is unavailable but the UI still shows fixed claims such as `Priority: High`, `Type: Feature`, `Est: 3h`, static workflow `Claude is writing code...`, and `12 tasks completed`.

## Required selected-project behavior

Add bounded shared session state to the Registry/project view model:

- `selectedProjectId` + setter or equivalent;
- clicking a project in Command Center rail selects it before navigation;
- clicking sidebar project shortcut selects it before navigation;
- Projects-card `Open cockpit` selects it;
- direct `/projects/:id` route should synchronize selected ID after successful registered resolution;
- returning to Command Center during session shows selected real project;
- if selection is removed/archived/unavailable, choose deterministic valid Registry fallback;
- never fall back to fixture identity in Tauri desktop mode.

## Required live-data truthfulness

Until M08/M09/M10 provides task intelligence:

- do not display fabricated priority/type/estimate;
- do not claim Claude/Codex is running unless real session/task evidence says so;
- do not show `12 tasks completed` or other fixture task counts as live Tauri facts;
- use neutral text such as `Task evidence not discovered yet`, `Workflow state unavailable`, `—`, or clearly labeled future-data placeholders;
- browser preview may keep isolated demo fixtures, but Tauri live mode must not mix them with real Registry identities.

Do not pull M08 task parsing into M07.06.

---

# C08 — FRONTEND FOCUSED TEST MATRIX — R09/R10/R11/R12

The existing 11-test suite is not enough. Add direct tests using Tauri-mode mocked APIs.

At minimum prove:

1. Command Center renders AI-Commerce-HQ from live Registry and not FormuLab identity;
2. project count reflects live mocked Registry count;
3. newly registered project refresh appears in Command Center rail;
4. sidebar shortcuts derive from live Registry IDs;
5. archive/remove refresh removes project from rail/shortcuts;
6. selected project remains Current Project after route navigation/back during session;
7. pending `/projects/:id` lookup renders Loading only and not unrelated project identity;
8. delayed AI-Commerce-HQ lookup never flashes FormuLab;
9. stale previous route result cannot overwrite newer route;
10. unknown registered ID yields error/not-found, not FormuLab;
11. Git-status async completion does not replace project identity;
12. Tauri live Current Task/Workflow/Engineering Brief does not show unsupported fixture claims;
13. Command icon opens Command Palette;
14. Sparkles opens AI Assistant;
15. Bell opens Notifications;
16. browser-preview fixture behavior remains explicitly isolated.

Update stale database mock schema expectations to current schema v7 where applicable.

Do not merely snapshot DOM; make assertions around identity/state transitions.

---

# C09 — UI FREEZE + MANUAL GATE

Preserve current user-directed composition:

- visible title only `Global Overview`;
- approx 220px sidebar;
- current-project compact header;
- real Registry project rail/shortcuts;
- distinct topbar surfaces;
- global centered Akilta footer with red heart;
- no outer Command Center scroll at canonical desktop sizes;
- no visible nested scrollbars on the user-specified compact panels.

Do not hide important content merely with `overflow:hidden`. If a direct/manual check shows clipping, compact or summarize truthfully rather than adding scrollbars.

No visual PASS unless user personally approves refreshed `.lnk` build.

---

# C10 — Full regression + final stable publication

Frontend from `H!veAI`:

```powershell
npm run typecheck
npm test -- --reporter=dot
npm run build
npm audit --audit-level=high
```

Rust/Tauri from Git root:

```powershell
cargo fmt --manifest-path H!veAI/src-tauri/Cargo.toml -- --check
cargo check --manifest-path H!veAI/src-tauri/Cargo.toml
cargo test --manifest-path H!veAI/src-tauri/Cargo.toml
cargo build --manifest-path H!veAI/src-tauri/Cargo.toml
```

Run the isolated launcher failure harness.

Then run the real publisher:

```powershell
powershell -ExecutionPolicy Bypass -File H!veAI\scripts\publish-dev-qa.ps1
```

Record:

- no-bundle build success;
- dedicated new frontend-ready marker;
- 5173 = 0 listeners;
- 8765 = 0 listeners;
- no visible console;
- candidate cleanup;
- stable-path readiness smoke;
- `.lnk` direct target + canonical icon;
- no installer.

---

# Tracker / docs truthfulness

Update `H!veAI/TASKS.md` additively:

- M07.05 strict audit = FAIL;
- M07.06 active until independent audit;
- M08 unstarted;
- user visual acceptance pending;
- M01 actual in-app restart remains manual/unverified unless genuinely tested through the in-app command.

Create:

`H!veAI/docs/migration/M07.06_FOCUSED_EVIDENCE_AND_RUNTIME_TRUTH_CLOSURE.md`

Do not edit historical Codex logs.

---

# Acceptance gate

M07.06 may report **PENDING HUMAN ACCEPTANCE** only if all automated items are true:

1. Git diff returns no `/dev/null` metadata and uses `--no-textconv`;
2. real diff product matrix passes;
3. dedicated frontend-ready command/ACL/post-render call restored;
4. marker has one intended Rust production emission site;
5. launcher rollback is SHA-256 proven;
6. isolated launcher failure harness passes;
7. watcher R01/R02 focused matrix passes;
8. Registry R03 identity matrix passes;
9. migration/SQLite R04/R05 matrix passes;
10. live Registry UI focused matrix passes;
11. selected Current Project session state works;
12. live Tauri UI contains no unsupported fixture task/workflow claims;
13. full frontend/Rust regression passes;
14. real stable publication passes;
15. M08 remains unstarted;
16. no installer was created.

If any automated MAJOR remains:

`M07.06 RESULT = FAIL`

If all automated gates close but visual/restart gates remain:

`M07.06 RESULT = PENDING HUMAN ACCEPTANCE`

Independent ChatGPT audit decides whether the automated gate is actually closed.

---

# Final response / log format

Return and record:

1. M07.06 RESULT
2. FETCH / SYNC / START HEAD / FINAL HEAD
3. C01 Git diff correction + exact tests
4. C02 readiness correction + ACL/source evidence
5. C03 launcher SHA/failure harness
6. C04 watcher exact focused tests
7. C05 Registry exact focused tests
8. C06 migration/SQLite exact focused tests
9. C07 selected-project + truthfulness changes
10. C08 frontend exact focused tests
11. C09 UI freeze / manual status
12. C10 full regression
13. real publisher evidence
14. 5173 / 8765 / console status
15. Desktop `.lnk` target/icon
16. installer status
17. files changed
18. exact test names/results, not aggregate counts only
19. commits/push/final remote HEAD
20. remaining automated findings
21. manual-only items
22. exact next step

Stop after M07.06. Do not create/start M08.
