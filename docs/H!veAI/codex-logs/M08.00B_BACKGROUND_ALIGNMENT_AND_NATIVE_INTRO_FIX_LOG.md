# M08.00B Background Alignment and Native Intro Fix Codex Log

Date: 2026-08-25
Branch: `H!veAI`
Active prompt: `docs/H!veAI/prompts/M08.00B_BACKGROUND_ALIGNMENT_AND_NATIVE_INTRO_FIX_PROMPT.md`

## Scope

This bounded remediation corrected the M08.00 presentation bootstrap defects.
M08 Task Source Discovery was not started, and no Windows installer was
created.

## Evidence and changes

- M08.00B was fetched from `origin/H!veAI` before execution.
- The canonical background remains byte-identical at
  `src/assets/hiveai-app-background.png`: SHA-256
  `7997ADD4EE7417B5818C1E2B7789B4C20D7B5F7EEC3215B9C0A136A5B9791C23`,
  1,668,436 bytes.
- The canonical opening video remains byte-identical at
  `src/assets/opening-video.mp4`: SHA-256
  `A438404A19CE53C45D1385BA1F1009E9AEA110C7361C42B278844EBCF76C6686`,
  1,980,989 bytes.
- Background ownership moved from the shell to the post-sidebar `.main-area`
  workspace. The sidebar no longer owns the canonical background layer.
- Startup intro is a fixed `100vw`/`100vh` overlay with `overflow:hidden`,
  `object-fit:contain`, no controls, and no normal-flow layout contribution.
- The React application mounts beneath the overlay and
  `hiveai_frontend_ready` remains independent of video completion.
- Native Rust `StartupIntroState` uses an atomic process-local claim. The first
  claim returns true, subsequent claims in the same process return false, and a
  newly constructed native process state claims fresh. Browser preview never
  invokes the native command.
- The command is exposed through the narrow
  `allow-startup-intro-claim` foundation permission and default capability.
- `sessionStorage` is not used by the startup intro lifecycle.
- Claim false, claim error, ended media, media error, route navigation, browser
  preview, fixed overlay layout, and frontend-ready independence are covered by
  `tests/m08.00-focused.test.tsx` (9/9).
- Rust startup claim tests pass (4/4); the complete Rust suite passes (102/102).

## Automated gates

PASS: `npm run typecheck`

PASS: `npm test -- --reporter=dot` (48/48)

PASS: `npm run build`

PASS: `npm audit --audit-level=high` (0 vulnerabilities)

PASS: `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`

PASS: `cargo check --manifest-path src-tauri/Cargo.toml`

PASS: `cargo test --manifest-path src-tauri/Cargo.toml` (102/102)

PASS: `cargo build --manifest-path src-tauri/Cargo.toml`

PASS: `scripts/tests/publish-dev-qa-failure-harness.ps1` (all isolated
temporary-directory failure and rollback cases)

PASS: `scripts/publish-dev-qa.ps1`, including Tauri production mode with
`--no-bundle`, PE validation, no forbidden development port, frontend-ready
smoke, no visible console host, SHA-256 candidate/stable equality, rollback
checks, and shortcut validation.

## Published manual-QA build

Stable executable:
`dev-bin/H!veAI.exe`

SHA-256:
`A827B05495A0C36197B39B30B4168CB8CC047C7BAB75FEF1F945F8F0A9F15488`

Size: 17,076,224 bytes

Desktop shortcut: `Desktop/H!veAI.lnk`

Shortcut target: `H!veAI/dev-bin/H!veAI.exe`

Shortcut icon: `H!veAI/dev-bin/H!veAI.ico` (canonical small-logo-derived
shortcut icon rule preserved)

## Final status

`M08.00B RESULT = PENDING USER MANUAL ACCEPTANCE`

The production-ready native build is published for the user’s manual launch
and visual/lifecycle inspection. Independent M08.00B audit and user acceptance
remain open. M08 Task Source Discovery remains blocked and unstarted.
