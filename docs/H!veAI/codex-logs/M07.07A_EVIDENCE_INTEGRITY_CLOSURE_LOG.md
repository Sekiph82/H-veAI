# M07.07A Evidence Integrity Closure Log

## Scope and synchronization

- Product: H!veAI. Repository: AI-Commerce-HQ. Branch: H!veAI.
- Ran `git fetch origin H!veAI`; local checkout was fast-forwarded from
  68e8e0f to 096b536 before reading the authoritative audit and prompt.
- Read AGENTS.md, the M07.07 strict audit, the historical M07.07 Claude log as
  claims only, the prior M07.07 prompt, and this M07.07A prompt.
- M07.07A is bounded evidence-integrity remediation. The dashboard, Git Diff,
  frontend-ready IPC, Registry production identity policy, and selected-project
  behavior were not redesigned or reimplemented.
- Historical M00-M07.07 logs are preserved unchanged. This log is the corrective
  record for claims the M07.07 strict audit found overstated.

## SQLite evidence

- `sqlite_busy_locked_contention_respects_configured_bounds` — exercises
  `DatabaseState::open_connection()` twice; connection A holds `BEGIN IMMEDIATE`,
  connection B performs a conflicting real INSERT, receives BUSY/LOCKED, and is
  measured at 4500-7000 ms. Classification: REAL_DB_FAILURE. PASS.
- `sqlite_backup_publication_failure_preserves_source_and_prior_backup` —
  exercises `create_migration_backup()` with a private cfg(test), thread-local
  publication failpoint after temporary backup production and before rotation;
  source/prior backup bytes and temporary cleanup are asserted. Classification:
  REAL_PRODUCTION_PATH_WITH_TEST_FAILPOINT. PASS.
- `sqlite_backup_failure_prevents_migration_mutation` — creates a real v6
  database with numeric snapshot timestamp and prior backup, injects the same
  publication failpoint through `DatabaseState::initialize()`, then proves
  initialization errors, migration max remains 6, timestamp remains numeric, and
  no v7 row exists. Classification: REAL_PRODUCTION_PATH_WITH_TEST_FAILPOINT.
  PASS.
- `sqlite_corrupt_db_preflight_fails_without_changing_source_bytes` and
  `sqlite_open_connection_reports_fk_wal_busy_timeout_and_synchronous` remain
  direct production-path regression evidence. PASS.

## Watcher evidence

- `watcher_attachment_failure_preserves_rescan_requirement` — exercises
  `WatcherManager::refresh_from_registry()` and `configure_project()` with a
  private cfg(test) attach failpoint; no watch handle/root is installed and the
  seeded rescan requirement remains. Classification:
  REAL_PRODUCTION_PATH_WITH_TEST_FAILPOINT. PASS.
- `watcher_git_refresh_failure_preserves_rescan_requirement` — uses a registered
  initialized/committed Git repository, emits GitMetadata, and injects failure at
  the internal Git snapshot call. No false snapshot is persisted and rescan stays
  required. Classification: REAL_PRODUCTION_PATH_WITH_TEST_FAILPOINT. PASS.
- `watcher_snapshot_persistence_failure_preserves_rescan_requirement` — injects
  failure immediately before the real project-snapshot INSERT; the error returns
  and in-memory rescan state remains set. Classification:
  REAL_PRODUCTION_PATH_WITH_TEST_FAILPOINT. PASS.
- `watcher_unchanged_refresh_does_not_duplicate_watcher` — compares actual
  `inner.watches` and `inner.watch_roots` identity before/after refresh. PASS.
- `watcher_repaired_root_refresh_reattaches_to_new_root` — performs Registry path
  repair, refreshes the manager, and asserts canonical new root, one handle, and
  no old root. Classification: REAL_PRODUCTION_PATH_WITH_REAL_STATE_TRANSITION.
  PASS.
- `watcher_event_on_repaired_root_is_observed` — writes under the repaired root,
  polls with a bounded five-second deadline, and proves last-event and persisted
  snapshot evidence changes. Classification: REAL_OS_EVENT. PASS.
- `watcher_git_category_event_persists_snapshot` and
  `watcher_non_git_event_does_not_persist_git_snapshot` use a real Git repository
  and assert Git snapshot linkage/count gating. Classification: REAL_DB/STATE.
  PASS.
- `watcher_manager_drop_releases_worker_and_watchers` retains inner state through
  Drop and proves running=false and both watcher maps empty. PASS.
- `watcher_symlink_escape_rejected_by_physical_containment` attempted a real
  Windows symlink. Classification: UNVERIFIED — link creation denied by
  environment: A required privilege is not held by the client. (os error 1314).
  The test did not substitute an ordinary outside file for a symlink.

## Frontend transition evidence

- `newly_registered_project_refresh_appears_in_rail` begins without fmcg, invokes
  the real Projects registration flow, mutates the mocked backend only on that
  operation, refreshes shared Registry state, then proves the rail appearance.
  Classification: REAL_UI_TRANSITION. PASS.
- `archive_remove_refresh_removes_from_rail_and_shortcuts` begins with fmcg,
  invokes the real archive control, updates backend state on operation, refreshes
  the provider, and proves removal from Projects, rail, and shortcuts. REAL_UI_TRANSITION. PASS.
- `stale_earlier_route_lookup_cannot_overwrite_newer_route` changes route inside
  one mounted BrowserRouter, resolves Bulk-Edit, then resolves stale
  AI-Commerce-HQ. Classification: REAL_UI_TRANSITION. PASS.
- `browser_preview_fixtures_remain_isolated_from_tauri_live_mode` positively
  asserts FormuLab preview identity, no live Registry list call, and no live
  AI-Commerce-HQ identity. Classification: REAL_UI_TRANSITION. PASS.
- Existing M07.06 selection, loading, unknown-ID, and async identity tests remain
  green. Focused frontend result: 20/20 PASS.

## Publisher evidence

- `failed_smoke_path_no_spawned_test_process` now starts a harmless bounded
  PowerShell child in the temp-only harness, simulates smoke failure, cleans up
  in finally, verifies the PID is gone, and verifies staged artifacts are absent.
  Classification: REAL_PROCESS_FAILURE_CLEANUP. PASS.
- The other eight isolated publisher cases remain temp-only and unchanged in
  scope: invalid candidate, build/provenance, readiness, shortcut precondition,
  post-swap SHA-256 rollback, locked stable, successful hash equality, and no
  bypass interface. Harness result: 9/9 PASS.

## Verification commands and results

- `cargo fmt --manifest-path src-tauri/Cargo.toml`: PASS.
- `cargo test --manifest-path src-tauri/Cargo.toml db::tests -- --nocapture`:
  9/9 PASS.
- `cargo test --manifest-path src-tauri/Cargo.toml watcher::tests -- --nocapture`:
  26/26 PASS, with the symlink case explicitly UNVERIFIED as recorded above.
- `npx vitest run tests/m07.06-focused.test.tsx --reporter=dot`: 20/20 PASS.
- Full frontend regression: 31/31 PASS; `npm run typecheck`, `npm run build`, and
  `npm audit --audit-level=high` PASS with 0 vulnerabilities.
- Full Rust regression: 98/98 PASS; `cargo fmt -- --check`, `cargo check`, and
  `cargo build` PASS.
- `publish-dev-qa-failure-harness.ps1`: 9/9 PASS.
- `publish-dev-qa.ps1`: PASS; Tauri production `--no-bundle` build was
  smoke-tested and published to `dev-bin/H!veAI.exe`. Desktop `H!veAI.lnk`
  targets that executable and uses `dev-bin/H!veAI.ico,0`.
- Independent strict audit remains required before M07.07A can leave ACTIVE
  status.
- No installer was created. M08 was not started or created.

## Truthful status

M07.07 remains FAIL under its strict audit. M07.07A is ACTIVE pending full
regression and independent audit. The symlink capability remains UNVERIFIED, and
the user visual Desktop shortcut and M01 in-app restart gates remain open. This
log does not claim human-only pending status or authorize M08.
