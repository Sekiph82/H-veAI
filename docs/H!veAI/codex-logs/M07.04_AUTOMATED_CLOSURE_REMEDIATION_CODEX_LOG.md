# M07.04 Automated Closure Remediation Codex Log

## Scope and synchronization

- Ran `git fetch origin H!veAI` before reading the active documents.
- Fast-forwarded `H!veAI` from `99c43d4` to fetched audit/prompt baseline `6afa03a`.
- Read the complete M07.03 independent strict audit and M07.04 prompt.
- Preserved the M07.03 single-viewport Command Center UI and all historical logs.
- Preserved unrelated untracked root files `start-demo.bat` and `task.md`.

## Remediation

- Rescan state is now computed before persistence and clears only after an
  available explicit reconciliation; failed/missing roots retain the flag.
- Non-Git projects retain repository identity metadata; no-remote repair uses
  fixed-argument read-only `git merge-base --is-ancestor` continuity.
- Added migration v7 for `project_snapshots.created_at` legacy epoch values and
  updated the native/UI schema expectation to v7.
- SQLite backup publication rotates bounded `.bak`/`.bak.prev` paths and
  restores the prior backup if publication fails.
- Binary fallback metadata is scoped to the current `diff --git` record, and
  the misleading raw test was renamed to explicitly test sanitizer behavior.
- Added the narrow `hiveai_frontend_ready` command, emitted only after React
  root render invokes it, logging `HIVEAI_FRONTEND_READY`.
- Hardened `publish-dev-qa.ps1`: it has no build bypass, always runs
  `npm run tauri:build -- --no-bundle`, waits for the new readiness marker with
  Vite absent, guarantees bounded candidate cleanup, validates the stable path,
  and retains rollback staging.

## Verification

- `cargo fmt --manifest-path H!veAI/src-tauri/Cargo.toml`: PASS.
- `cargo check --manifest-path H!veAI/src-tauri/Cargo.toml`: PASS.
- `cargo test --manifest-path H!veAI/src-tauri/Cargo.toml`: PASS, 50 tests.
- `npm run typecheck`: PASS.
- `npm test -- --reporter=dot`: PASS, 11 tests.
- `npm run build`: PASS.
- `npm audit --audit-level=high`: PASS, 0 vulnerabilities.
- `cargo build --manifest-path H!veAI/src-tauri/Cargo.toml`: PASS.
- Hardened `scripts/publish-dev-qa.ps1`: PASS; it ran the production
  `--no-bundle` build, observed `HIVEAI_FRONTEND_READY` after database IPC,
  rejected forbidden ports, cleaned candidate processes, and published the
  stable path with target/icon validation.
- Final UI status remains **PENDING USER VISUAL ACCEPTANCE**.
- M01 actual in-app restart remains manual/unverified.
- No installer, M08 prompt, or M08 implementation was created.
