# M07.03 Consolidated Strict Closure Codex Log

## Scope

M07.03 is the single authoritative remediation pass for the M07.02A strict
launcher audit, the incorporated M07.02B requirements, and the consolidated
single-viewport UI gate. No installer or later milestone work was started.

## Synchronization and evidence

- Ran `git fetch origin H!veAI` before reading the active prompt.
- Started from branch `H!veAI`, fast-forwarded local `HEAD` from `0cca175` to
  `ea3c8a7`, and preserved unrelated user files `start-demo.bat` and `task.md`.
- Read `M07.02A_DEV_QA_LAUNCHER_STRICT_AUDIT.md`, the M07.03 prompt,
  `UI_LAYOUT_GOVERNANCE.md`, and the governing repository instructions.
- Historical M00-M07 and M07.01/M07.02 logs were preserved unchanged.

## Implemented closure work

- Recomposed the command center into a single desktop viewport with a dominant
  FormuLab cockpit, projects rail, AI brief/assistant rail, compact system
  status, and internal detail scrolling.
- Used the canonical H!veAI small logo and text-logo assets in the shell.
- Added migration v6 for residual timestamp standardization and updated the
  database status contract to the latest schema.
- Replaced raw SQLite migration file copying with rusqlite's SQLite backup API,
  retaining atomic temporary publication and bounded backup naming.
- Added physical containment validation for watcher paths while preserving
  lexical fail-closed behavior for unavailable test roots.
- Allowed a moved Git checkout to advance HEAD when its sanitized remote
  identity matches; no-remote repositories still require matching HEAD evidence.
- Removed binary patch payloads at the Git diff product boundary while retaining
  binary-file metadata.
- Replaced the QA publisher with a production `npm run tauri:build -- --no-bundle`
  helper that candidate-smokes before stable replacement, checks ports/title/
  console behavior, validates the shortcut/icon, and restores the prior stable
  binary on publication failure.
- Updated AGENTS and TASKS with UI governance, launcher provenance, rollback,
  and the human visual-acceptance gate.

## Verification

- `npm run test`: PASS, 11 tests.
- `npm run typecheck`: PASS.
- `npm run build`: PASS.
- `cargo fmt --manifest-path src-tauri/Cargo.toml`: PASS.
- `cargo test --manifest-path src-tauri/Cargo.toml`: PASS, 50 tests.
- `npm run tauri:build -- --no-bundle`: PASS; production candidate created.
- `scripts/publish-dev-qa.ps1 -SkipBuild`: PASS; candidate smoke-tested and
  published to `dev-bin/H!veAI.exe`.
- Shortcut target verified as `dev-bin\\H!veAI.exe`; icon verified as stable
  `dev-bin/H!veAI.ico,0`; no H!veAI listener on ports 5173 or 8765 was present.

## Acceptance state

Automated remediation evidence is locally complete for the implemented scope.
The final UI status is intentionally **PENDING USER VISUAL ACCEPTANCE**. The
user must inspect the refreshed Desktop `H!veAI.lnk` build. No visual PASS is
claimed from automated build or process evidence.
