# M19 Next Best Task AI + Engineering Brief V06 — Builder Evidence Log

Date: 2026-09-17 (Europe/Istanbul)

## Authority and safe synchronization

- Authoritative prompt: `docs/H!veAI/prompts/M19_NEXT_BEST_TASK_AI_ENGINEERING_BRIEF_V06_REMEDIATION_PROMPT.md`
- V05 strict audit and the preceding V03/V04/V05 remediation history were read before source changes.
- Safe synchronization: `git fetch origin main` followed by `git merge --ff-only origin/main`.
- Synchronized start SHA / `origin/main`: `48501f5293b3ae2c8749a25dea9eeb4a66ee1525`
- Implementation commit: `b8ea73e` (`Complete M19 V06 production remediation`)
- This log is created after implementation and is immutable; it is not amended after publication.

## Scope guard

- `TASKS.md` and `CODEX_ROADMAP.md` were not changed.
- M20 was not started.
- No remote GitHub mutation, force-push, reset, rebase, or history rewrite was used.
- Builder evidence does not claim owner-native interactive acceptance. Owner acceptance remains pending independent source/audit PASS.

## V05 strict finding dispositions

### F-M19-V05-STRICT-001 — production-path fair acquisition

Closed at source level. `GitHubAcquisitionContext` and tiered planning now feed the production `snapshot_for_intent` path. A native `hiveai_github_portfolio_snapshots` command exposes the same shared orchestration for Idle, Selected, Navigation, and Manual intents. Navigation uses deterministic round-robin primary service and a process-wide governor; the native evidence probe exercised eight real registry GitHub identities through that orchestration and observed 40 admitted primary stages.

### F-M19-V05-STRICT-002 — portfolio tracking storm and manual scope

Closed at source level. Selected cadence is 300 seconds and background portfolio cadence is 3600 seconds. Manual refresh carries an explicit project id and schedules only that project. Tracking requests use a process-wide 40-stage/hour governor, 20-second HTTP timeout, bounded worker scope, and rotation. Exact changed-HEAD calculations are 24/27/30/60 stages/hour for 8/9/10/20 background projects, with 40 admitted at the 20-project boundary.

### F-M19-V05-STRICT-003 — pre-refresh M19 history baseline

Closed at source level. Native `refresh_and_compare` captures the M19 snapshot before awaiting the scoped refresh, records that exact pre-refresh fingerprint once after successful completion, and then computes the current snapshot. Failed refreshes return without recording a misleading baseline. Native tests cover changed, unchanged, and failed refresh semantics; the mounted frontend test proves no legacy history command is issued while refresh is in flight.

### F-M19-V05-STRICT-004 — optional 403/429 process-wide circuit

Closed at source level. Optional enrichment classifies 403/429 as rate limited, opens the same process-wide circuit as primary acquisition, stops remaining calls, and subsequent snapshots fail closed to stale/unavailable cache state. Native production-path tests cover both 403 and 429 and assert the second snapshot emits zero transport requests.

### F-M19-V05-STRICT-005 — generated GitHub acquisition evidence

Closed at source level. `docs/H!veAI/evidence/M19_V06_GITHUB_ACQUISITION_MATRIX.md` was generated by `npm run generate:evidence:v06`, which runs the native production orchestration probe and parses its machine-emitted evidence marker. It contains per repository/resource/stage rows with cache, admission, coalescing, HTTP status, failure class, budget-before/after, and fetched/last-good fields, plus exact 8/9/10/20 hourly calculations. It is redacted and contains no credentials or raw response bodies.

### F-M19-V05-STRICT-006 — real React geometry surface

Closed at source level. `npm run test:geometry:browser` starts Vite, injects only a bounded Tauri invoke seam, navigates to the actual `/projects` route, and evaluates the production `App` → `RegistryProvider` → `Projects` → `ProjectRegistryCard` tree in Chromium. It does not clone the markup or mock geometry APIs. 3 cards passed footer containment, action non-overlap, `Open cockpit`, `Local workspace`, horizontal overflow, and keyboard focus assertions at requested 1536, 1280, and 640 widths (measured inner widths 1510, 1254, 614 because of the browser scrollbar).

### F-M19-V05-STRICT-007 — publication and acceptance boundary

Closed for builder publication evidence; owner acceptance remains pending. Native Tauri publication completed through `scripts/publish-dev-qa.ps1`, including the production build and smoke publication. Published executable: `dev-bin/H!veAI.exe`, SHA-256 `4B8154848E435F4418F19F929B5CC431733FF72E687F254D591DD55FAC32CA80`. This log intentionally records source/publication evidence only and does not self-accept the owner-native interactive gate.

## Verification gates

- `cargo check --manifest-path src-tauri/Cargo.toml` — PASS.
- `cargo test --manifest-path src-tauri/Cargo.toml --lib --no-fail-fast --quiet -- --test-threads=1` — PASS, 555/555.
- `npm run typecheck` — PASS.
- `npm run build` — PASS.
- `npm test -- --maxWorkers 1 --minWorkers 1` — PASS, 19 files / 164 tests.
- `npm run test:geometry:browser` — PASS on the real React Projects route at all three requested viewport classes.
- `npm run generate:evidence:v06` — PASS; generated acquisition matrix present.
- `powershell -ExecutionPolicy Bypass -File scripts/publish-dev-qa.ps1` — PASS; published native executable and smoke publication completed.
- `git diff --check` — PASS.

## Publication status

The final publication commit contains this immutable log and the generated V06 acquisition evidence artifact. Final commit SHA and GitHub URLs are supplied at handoff after non-force push and live SHA equality verification. M19 remains audit-pending until independent strict review and owner-native acceptance; no M20 work is included.
