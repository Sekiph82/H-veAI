# M07.07B Native Restart Manual Acceptance

Date: 2026-08-25

## Verdict

`PASS — REAL NATIVE RESTART USER-ACCEPTED`

The user manually exercised the published stable H!veAI desktop application through the real Settings surface and confirmed that the restart works correctly.

Observed user flow:

1. Opened `Settings` in the native H!veAI desktop app.
2. Clicked `Restart H!veAI`.
3. The confirmation prompt `Restart H!veAI now?` appeared.
4. The user confirmed the prompt.
5. H!veAI exited/relaunched and returned to a usable Command Center window.
6. No browser-hosted H!veAI shell was used for the restart.
7. No visible terminal/cmd/PowerShell restart surface was observed in the supplied evidence.

The repository implementation previously verified for this path is:

`Settings -> invoke("hiveai_request_restart") -> app.request_restart()`

This manual action closes the previously outstanding real in-app restart acceptance gate for both the M01 lifecycle gate and the M07 final restart gate.

## Remaining M07 human gate

The restart gate is closed. The only remaining user-facing M07 acceptance item is the enlarged one-piece sidebar H!veAI logo after M07.07C is implemented and republished.

Do not ask the user to repeat the restart test in M07.07C unless the implementation changes the restart path or an independent regression demonstrates it is broken.
