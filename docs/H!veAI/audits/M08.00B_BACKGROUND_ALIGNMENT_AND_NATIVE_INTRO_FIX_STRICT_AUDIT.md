# M08.00B Background Alignment and Native Intro Fix Strict Audit

Date: 2026-08-25

## Verdict

`PASS`

M08.00B closes the bounded presentation remediation. No production BLOCKER or MAJOR finding remains. The user's native visual/lifecycle acceptance is PASS and is recorded separately in `M08.00B_MANUAL_ACCEPTANCE.md`.

M08 Task Source Discovery was not implemented by this remediation.

## Audited implementation

Prompt/base commit:

`2b1fd9362a95eb31c614255d18a91e437cfc7b6d`

Implementation/log commit:

`492b2633c8f16b9a4897b0080d23fedece9aaa7d`

The compare is one bounded commit. Changed production scope is limited to presentation lifecycle, narrow Tauri ACL/state wiring, focused tests, governance/tracker, and the new immutable builder log. No PNG or MP4 asset bytes changed.

## Acceptance matrix

### B01 — Background belongs to post-sidebar workspace

`PASS`

Production CSS removes the canonical image layer from `.app-shell::before` and attaches it to `.main-area::before` with `center center / cover no-repeat`. `.main-area` is the post-sidebar workspace container, so the hive/bee composition is centered relative to the main workspace rather than the full window.

The user-provided final native screenshot confirms the sidebar remains visually separate and the hive artwork is centered in the working region.

### B02 — Startup intro is a real viewport overlay

`PASS`

Production CSS defines `.startup-intro` as a fixed overlay with `inset:0`, `100vw`, `100vh`, `overflow:hidden`, a deep-navy background, and high z-index. The video fills the overlay with `object-fit:contain`, no controls, and no normal-flow layout contribution.

The user's startup screenshots show the MP4 occupying the H!veAI client area and then yielding to the usable dashboard.

A document/host scrollbar is visible in the captured non-maximized native window, but it is not evidence of the earlier video element expanding normal document flow: it remains present after the intro and the user explicitly accepted the visual result. No M08.00B remediation is required for this accepted host/window behavior.

### B03 — Native process-scoped intro ownership

`PASS`

`StartupIntroState` is process-local Rust state backed by `AtomicBool`. The first claim returns true, subsequent claims in the same process return false, and a fresh state begins unclaimed. It is registered with Tauri during native application setup.

The dedicated `hiveai_startup_intro_claim` command uses the production state helper. No SQLite, filesystem, registry, localStorage, or sessionStorage persistence owns the lifecycle.

### B04 — Narrow command registration and ACL

`PASS`

`hiveai_startup_intro_claim` is explicitly registered in the Tauri invoke handler. `allow-startup-intro-claim` is a dedicated foundation permission and is added narrowly to the main-window capability. No broad allow-all capability was introduced.

### B05 — Frontend claim flow and failure safety

`PASS`

`StartupIntro` detects the established Tauri boundary, begins in a safe pending overlay state, asynchronously invokes the native claim, plays only when the claim returns true, skips cleanly when false, and fails open to a usable application on command error.

The natural media `ended` path and media-error path dismiss through the bounded fade. The 15-second failsafe remains present. The underlying `<App />` stays mounted while the overlay is active.

### B06 — Frontend-ready independence

`PASS WITH MINOR EVIDENCE NOTE`

`main.tsx` keeps `<App />` and `<StartupIntro />` as siblings and emits `hiveai_frontend_ready` from the existing post-mount effect independently of intro completion.

The focused test checks this relationship by inspecting production source rather than timing a fully mounted `main.tsx` runtime invocation. The source architecture is direct and the publisher readiness smoke reportedly passes, so this is a MINOR evidence note only.

### B07 — Focused frontend evidence

`PASS WITH MINOR COVERAGE NOTE`

The focused suite directly covers:

- native claim true -> intro shown while App is mounted;
- claim false -> skip with no playback;
- claim failure -> fail open;
- media ended -> fade/remove;
- media error -> fade/remove;
- route navigation -> no second native claim;
- browser preview -> no native claim;
- CSS background/overlay ownership and no sessionStorage;
- frontend-ready source independence.

The active prompt asked for an explicit project-switching no-second-claim test as well. The focused file does not contain a separate project-switching case. Production ownership is root/process-scoped, route navigation is directly covered, and the user manually accepted lifecycle behavior, so this remains MINOR rather than a production failure.

### B08 — Rust claim evidence

`PASS`

Four Rust tests exercise the process-local claim semantics: first claim true, second claim false, fresh state true again, and production helper reuse. The builder log reports 4/4 focused claim tests and 102/102 complete Rust tests.

### B09 — Automated regression and publication

`PASS`

The builder log reports:

- TypeScript typecheck PASS;
- frontend tests 48/48 PASS;
- frontend build PASS;
- npm audit high PASS with 0 vulnerabilities;
- Rust fmt/check/build PASS;
- Rust tests 102/102 PASS;
- publisher failure harness PASS;
- production Tauri `--no-bundle` publication/smoke PASS;
- stable EXE SHA-256 `A827B05495A0C36197B39B30B4168CB8CC047C7BAB75FEF1F945F8F0A9F15488`.

### B10 — Scope and closed-M07 preservation

`PASS`

The commit does not implement Task Source Discovery, task parsing, state machine work, agents, installer work, or M09+. It does not modify Registry, Git Engine, Watcher, SQLite, canonical logo assets, canonical background bytes, or MP4 bytes.

The accepted M07 one-piece logo/sidebar behavior remains intact in the user's final screenshot.

### B11 — Builder log truthfulness

`PASS WITH MINOR DOCUMENTATION NOTE`

The log accurately describes the core production changes and test totals. It does not record the synchronized base HEAD and final local/remote HEAD values as explicitly requested by the prompt. GitHub independently establishes that the implementation commit is a single child of the authoritative prompt commit and was the branch HEAD before audit writes. This omission does not affect production correctness.

### B12 — User manual acceptance

`PASS`

The user supplied native startup frames and the final Command Center screenshot and explicitly stated that manual acceptance is complete. This closes the remaining visual/lifecycle gate for M08.00B.

## Findings

### BLOCKER

None.

### MAJOR

None.

### MINOR

1. Focused frontend evidence has no separate project-switching no-reclaim test.
2. Frontend-ready independence test is source-structural rather than a direct mounted timing assertion.
3. Builder log omits exact synchronized-base/final-local/final-remote HEAD values requested by the prompt.

These are documentation/evidence quality notes and do not require another remediation pass because production source is coherent and the user has completed manual acceptance.

## Confidence and regression risk

Confidence: `HIGH`

Regression risk: `LOW`

The changes are narrow, process-local, root-level, and bounded to presentation lifecycle plus ACL wiring. Full frontend/Rust/publisher regression is reported green and the native build has been manually accepted.

## Final status

`M08.00 = PASS/CLOSED`

`M08.00B = PASS/CLOSED`

`M08 Task Source Discovery = AUTHORIZED / NOT YET IMPLEMENTED`
