# H!veAI M07.07A STRICT AUDIT

## VERDICT

**CONDITIONAL / PENDING USER VISUAL + M01 RESTART ACCEPTANCE**

M07.07A closes the automated MAJOR evidence-integrity findings from the M07.07 strict audit. No automated BLOCKER or MAJOR remains in the scoped M07.07A contract by source/test inspection.

M08 remains blocked until the two explicit human gates are completed:

1. user visual acceptance of the refreshed Desktop `H!veAI.lnk` production build;
2. actual M01 in-app restart full-cycle acceptance through `hiveai_request_restart -> app.request_restart()`.

The Windows physical symlink/junction containment capability remains **UNVERIFIED** because the environment denied link creation with OS error 1314. This is an explicitly permitted environment-limited outcome in the M07.07A prompt and is not being converted into a false PASS.

## A. BRANCH / SCOPE

- Branch: `H!veAI`
- M07.07A prompt/audit base: `096b53668698be4a8092a3c88da7b98f89af7303`
- M07.07A implementation/log HEAD: `c2186fbcf9a51792d0cbadb7ea6a2cf3beb5e44e`
- One implementation commit.
- Changed scope is bounded to TASKS/log, SQLite tests/failpoint, watcher tests/failpoints, frontend focused tests, and publisher failure harness.
- No dashboard production UI file, Git Diff implementation, frontend-ready IPC implementation, Registry production identity implementation, installer, or M08 implementation was changed in this pass.

## B. ACCEPTANCE MATRIX

### A01 SQLite real busy/locked contention — PASS

The test now uses two production `DatabaseState::open_connection()` connections. One holds `BEGIN IMMEDIATE`; the second performs a real conflicting INSERT. The test requires BUSY/LOCKED and bounds elapsed time to 4500-7000 ms before rolling back the holder.

### A02 SQLite backup-publication failure — PASS with NOTE

A private `#[cfg(test)]` thread-local failpoint is placed after logical temporary backup completion and before backup rotation/publication. It is not reachable through public runtime API, IPC, CLI, configuration, or environment variables.

The test creates a prior good `.bak`, injects publication failure, asserts an error, asserts the prior backup remains byte-for-byte unchanged, and asserts temporary cleanup.

**NOTE:** the source-byte assertion is captured before the deliberate second logical INSERT while the DB is operating in WAL mode. A direct post-failure logical row-count assertion would be clearer evidence of source logical integrity. No production mutation defect is exposed by source inspection, because `create_migration_backup()` reads the source connection and writes/renames only backup-side paths. This is not classified MAJOR.

### A03 Backup failure blocks migration mutation — PASS

A real v6 fixture is built with a numeric `project_snapshots.created_at`. The same private publication failpoint is exercised through `DatabaseState::initialize()`. Initialization fails, migration max remains 6, the numeric timestamp remains unchanged, and no version-7 migration row exists.

### A04 Watcher real failure/event/lifecycle evidence — PASS / one environment-limited UNVERIFIED

- attachment failure: PASS. Private test-only attach failpoint traverses `refresh_from_registry -> configure_project`, returns failure, preserves `rescan_required`, and installs no watcher/root.
- Git refresh failure: PASS. Real initialized Git repo + GitMetadata event + private Git-refresh failpoint returns error and preserves rescan state without false project-snapshot reconciliation.
- project-snapshot persistence failure: PASS. Private failpoint immediately before production INSERT returns error and leaves rescan state set.
- unchanged refresh duplicate prevention: PASS. Actual `inner.watches` and `inner.watch_roots` are compared.
- repaired-root reattachment: PASS. Registry repair + refresh proves new canonical root, exactly one watcher, and no old active root.
- repaired-root event observation: PASS. A real file write under the new root is followed by bounded polling until watcher event evidence and persisted snapshot count advance.
- Git-category snapshot gating: PASS. Real Git repo produces a linked non-null `git_snapshot_id`; FK semantics require an actual persisted Git snapshot.
- non-Git-category gating: PASS. Real Git project + Source event leaves Git snapshot count unchanged.
- manager Drop: PASS. Retained inner state proves `running=false` and empty watcher/root maps after Drop returns.
- physical symlink escape: **UNVERIFIED**. The test attempts a real Windows symlink and records the environment privilege failure (`os error 1314`) rather than substituting an ordinary outside file.

### A05 Frontend real Registry transitions — PASS

The four weak M07.07 tests were materially strengthened:

- new project is absent initially, then real Add Project UI flow mutates mocked backend state, provider refreshes, and the same mounted app later shows the new name in Command Center rail;
- archive starts from a visible project, real archive UI changes backend state, provider refreshes, and the project disappears without app restart;
- stale route race now occurs in one mounted router instance; stale AI-Commerce-HQ resolution cannot replace the later Bulk-Edit route;
- browser preview positively shows FormuLab preview identity while live AI-Commerce-HQ identity is absent and Tauri live registry behavior is not used as the preview authority.

Existing selection/loading/unknown-ID/async identity tests remain complementary regression coverage.

### A06 Publisher spawned-process cleanup — PASS

The temp-only failure harness now actually spawns a harmless PowerShell child, simulates post-spawn smoke/readiness failure, kills it in bounded cleanup, verifies the PID is gone, and confirms no staged artifact remains. It does not launch or kill the real H!veAI stable executable.

The other mapped publisher cases remain bounded to the generated temp directory. Production publication remains separate.

### A07 log / tracker truthfulness — PASS for current builder state

The new M07.07A log corrects the historical overclaims rather than rewriting the M07.07 Claude log. It records exact classifications and explicitly labels the symlink case UNVERIFIED. TASKS keeps M07.07 FAIL, M07.07A active pending independent audit, and M08 blocked.

After this independent audit, TASKS is conservative rather than falsely optimistic; it may be advanced to human-gate pending status only after this audit record is committed.

## C. REGRESSION / BUILD EVIDENCE

Builder log reports:

- focused DB tests: 9/9 PASS;
- focused watcher tests: 26/26 PASS, with the symlink capability explicitly recorded as UNVERIFIED;
- focused frontend suite: 20/20 PASS;
- full frontend regression: 31/31 PASS;
- full Rust regression: 98/98 PASS;
- TypeScript typecheck: PASS;
- frontend build and npm audit high threshold: PASS;
- cargo fmt/check/build: PASS;
- isolated publisher harness: 9/9 PASS;
- real production `--no-bundle` publication: PASS.

This audit independently inspected the changed source/test paths and did not treat aggregate counts as sufficient evidence. The execution commands themselves were not re-run by the auditor environment.

## D. SECURITY / SAFETY

- New failpoints are compile-time `#[cfg(test)]` only and do not expose a runtime/public bypass.
- No new arbitrary shell IPC or arbitrary Git execution boundary was introduced.
- Failure harness stays temp-only.
- No installer was created.
- M08 was not started.
- Physical symlink containment remains environment-limited UNVERIFIED, explicitly recorded rather than falsely passed.

## E. RESIDUAL FINDINGS

### BLOCKER

None.

### MAJOR

None.

### MINOR / NOTE

1. SQLite A02 would be clearer if it also queried the post-failure logical source contents after the deliberate second INSERT, rather than relying partly on main-file byte comparison under WAL.
2. The Git-category watcher test proves persisted Git linkage via non-null FK-backed `git_snapshot_id`; an explicit before/after `git_snapshots` row-count assertion would be even more direct, but the current evidence is sufficient when combined with source inspection.
3. The Windows symlink/junction escape case remains UNVERIFIED because the environment lacks link-creation privilege. This is an allowed prompt outcome, not a PASS.

## F. HUMAN ACCEPTANCE REQUIRED

### UI visual gate

User must launch the refreshed stable Desktop `H!veAI.lnk` build and visually confirm at normal/maximized desktop scale:

- no outer/body horizontal or vertical scroll;
- Global Overview remains one primary viewport;
- Projects rail shows names only;
- project click updates Current Project in place;
- `Open cockpit` is the navigation action;
- canonical small-logo + text-logo composition is intact;
- compact System Status remains in the right rail;
- Akilta footer is correctly positioned;
- no FormuLab flash/fallback when opening a live registered project.

### M01 restart gate

Cold close/reopen via `.lnk` does not satisfy M01. The real native restart path must be exercised end-to-end and H!veAI must return healthy after `app.request_restart()`.

## G. CONFIDENCE / RISK

- Audit confidence: **HIGH** for changed source/test evidence.
- Regression risk: **LOW-MEDIUM**.
- Human visual/runtime acceptance: **UNVERIFIED** until performed by the user.

## FINAL

**M07.07A RESULT = PENDING USER VISUAL + M01 RESTART ACCEPTANCE**

Automated closure is sufficient to stop remediation churn. Do not create another automated remediation prompt unless the user visual/restart checks reveal an actual defect.

**Do not start M08 until the human gates are completed.**
