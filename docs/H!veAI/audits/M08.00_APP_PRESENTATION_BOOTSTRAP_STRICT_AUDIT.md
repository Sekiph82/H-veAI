# M08.00 App Presentation Bootstrap Strict Audit

Date: 2026-08-25

## Verdict

`CONDITIONAL — AUTOMATED/SOURCE CLOSURE ACCEPTED; USER VISUAL + NATIVE LIFECYCLE ACCEPTANCE REQUIRED`

M08.00 is not M08 Task Source Discovery. The implementation remains correctly bounded to presentation/bootstrap work. M07 remains closed.

## Audited implementation

Implementation commit:

`3621424b3508c39cdd91704990d3c46d8a34c1ba`

Builder log commit follows the implementation and branch HEAD at audit start was:

`e41afb07739fed34cdd53b97c5ce92220965f083`

Base-to-implementation compare is one commit with the expected bounded file set only.

## Acceptance matrix

### A01 — Canonical global background asset

`PASS / USER VISUAL ACCEPTANCE STILL REQUIRED`

Repository asset exists at:

`H!veAI/src/assets/hiveai-app-background.png`

The implementation uses one root-level `.app-shell::before` background layer with cover/no-repeat and a dark overlay. It is not implemented route-by-route.

The builder log records matching source/repository SHA-256 and byte size. The Windows canonical source itself is outside GitHub, so independent audit cannot re-hash that source remotely. Final identity/appearance therefore remains subject to the user's native-build visual inspection.

### A02 — Opening video asset and in-window overlay

`PASS / USER NATIVE LIFECYCLE ACCEPTANCE STILL REQUIRED`

Repository asset exists at:

`H!veAI/src/assets/opening-video.mp4`

`StartupIntro.tsx` imports the bundled asset directly and renders a single in-window `<video>` overlay. It does not launch a browser, external player, terminal, or second Tauri window.

Video properties are correctly bounded:

- autoplay enabled;
- muted enabled;
- playsInline enabled;
- preload `auto`;
- controls disabled;
- no loop;
- natural `ended` event dismisses the overlay;
- media error dismisses the overlay;
- 15 second failsafe prevents a permanent startup trap;
- short 280 ms closing fade state.

### A03 — App mounts immediately behind intro

`PASS`

`main.tsx` renders `<App />` and `<StartupIntro />` as siblings inside `FrontendReady`. The existing post-mount `hiveai_frontend_ready` invoke remains in its own `useEffect` and is not gated on video completion.

This preserves the publisher/readiness contract while the intro is visible.

### A04 — Play-once semantics within one frontend lifecycle

`PASS / RESTART REPLAY UNVERIFIED UNTIL USER TEST`

The intro is gated by:

`hiveai.startup-intro.played`

in `sessionStorage` and by the native Tauri boundary. This prevents replay after a component remount within the same WebView/session and does not couple playback to the route pathname.

Focused tests directly prove browser-preview skip and same-session remount suppression.

Whether the production WebView2 session storage is recreated exactly as expected by the real `app.request_restart()` lifecycle is intentionally a manual acceptance item. Do not mark restart replay PASS until the user performs it in the published desktop application.

### A05 — Browser preview behavior

`PASS`

`StartupIntro` returns no overlay when the Tauri boundary is absent. Focused test `browser_preview_skips_native_intro_playback` verifies that media playback is not attempted in browser preview.

### A06 — Media end/error recovery

`PASS`

Focused tests exercise both `ended` and `error` paths and verify that the overlay disappears while the underlying App remains usable.

### A07 — Route stability / no ordinary navigation replay

`PASS WITH SOURCE + SESSION-GUARD EVIDENCE`

The intro is mounted once at the root, outside route content, and is not keyed from route/path/project state. The same-session guard also prevents remount replay. The global application shell remains shared across route transitions in focused coverage.

Manual native verification is still required for route navigation, project switching, minimize/restore and other ordinary desktop interactions.

### A08 — Neon liquid glass adaptation

`PASS / USER VISUAL ACCEPTANCE STILL REQUIRED`

The implementation adds bounded glass/glow design tokens and adapts:

- primary buttons;
- secondary buttons;
- focus-visible and disabled states;
- real `table` and `.queue-table` surfaces;
- headers and hover states;
- sidebar/topbar translucency;
- selected shared panels and Command Center surfaces.

The styling uses bounded blur/shadow, includes `prefers-reduced-motion`, and does not add continuous glow/pulse animation or WebGL/canvas effects.

Visual intensity, readability, clipping, hover quality and WebView2 smoothness remain manual acceptance items.

### A09 — M07 UI contracts preserved in source

`PASS`

Current `command-center.css` still preserves the accepted one-piece width-driven sidebar logo, 220 px sidebar override, names-only project rail behavior styles, bounded Command Center layout and footer rules. M08.00 adds only glass/readability overrides at the end of the stylesheet.

Full frontend regression is reported green by the builder log.

### A10 — Scope boundary

`PASS`

Compare from `c69957f94569c146a13531ca0fee960ab535b2d2` to implementation commit shows only the expected presentation/bootstrap files. No Git Engine, Watcher, SQLite, Registry, migration, native restart backend, installer, M08 Task Source Discovery, M09 or later implementation was introduced.

### A11 — Automated verification claims

`PASS WITH ONE MINOR EVIDENCE NOTE`

Builder log reports:

- focused M08.00: 6/6 PASS;
- full frontend: 45 PASS;
- typecheck PASS;
- build PASS;
- npm audit high PASS, 0 vulnerabilities;
- Rust fmt/check/tests/build PASS;
- Rust tests: 98 PASS;
- publisher failure harness: 9/9 PASS;
- production no-bundle publication and smoke PASS.

The prompt requested automated proof that `hiveai_frontend_ready` is not delayed behind video completion. The focused M08.00 file does not contain a dedicated test that invokes `main.tsx` and asserts this timing relationship. The production source makes the independence explicit, and the published readiness/smoke gate reportedly passes, so this is classified `MINOR / NOTE`, not a production blocker.

## Findings

### MAJOR

None demonstrated by repository source.

### MINOR

1. The focused M08.00 test matrix does not directly assert `hiveai_frontend_ready` firing while the intro remains visible. Source architecture and publisher smoke support the behavior, so no remediation is required before user acceptance unless the native build exposes a problem.

### UNVERIFIED / MANUAL GATES

1. Canonical background visual identity in the user's real build.
2. Cold launch plays the opening MP4 exactly once.
3. Video remains uncropped/undistorted and has no controls.
4. Fade completes cleanly and app remains usable.
5. Background remains visible while UI stays readable.
6. Glass/glow intensity is visually acceptable and does not feel like a gaming HUD.
7. Route navigation does not replay the intro.
8. Project selection does not replay the intro.
9. Minimize/restore does not replay the intro.
10. Real `Settings -> Restart H!veAI -> app.request_restart()` causes the intro to play again exactly once.
11. No browser/terminal appears.
12. Approved M07 Command Center geometry remains visually intact.

## Final gate

`M08.00 = CONDITIONAL / PENDING USER ACCEPTANCE`

Do not start M08 Task Source Discovery yet.

If the native visual/lifecycle checklist passes, M08.00 can be closed without another implementation pass and M08 Task Source Discovery may begin. If any manual item fails, create only a bounded M08.00 remediation prompt for the observed defect.