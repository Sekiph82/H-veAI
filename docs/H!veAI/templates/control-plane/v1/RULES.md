# Shared Control Plane Rules

Shared state semantics do not grant identical write permissions. Preserve the project-specific rules, canonical task authority, owner gates, and provider permissions that are already in force.
